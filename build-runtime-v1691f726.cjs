const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f725.cjs');
const MOD=path.join(ROOT,'degree-shadow-calibrator-v1691f726.js');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
for(const f of[BASE,MOD])if(!fs.existsSync(f))throw new Error('[F60.94.31.6] missing '+path.basename(f));
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
let app=fs.readFileSync(APP,'utf8');
if(!app.includes('getSamples:()=>lastSamples'))throw new Error('[F60.94.31.6] base sample exposure missing');
let mod=fs.readFileSync(MOD,'utf8');
for(const t of['DEGREE-SHADOW-CALIBRATOR-V16.9.1F60.94.31.6','HAM → KALİBRE · GÖLGE MOTORU','Pist → Bakım → Hava → Rail/Bariyer','splitChronological','MIN_N=30'])if(!mod.includes(t))throw new Error('[F60.94.31.6] module invariant missing '+t);
for(const bad of['new MutationObserver','setInterval(','document.addEventListener(\'touchend\'','document.addEventListener(\'pointerup\''])if(mod.includes(bad))throw new Error('[F60.94.31.6] forbidden global loop '+bad);
new Function(mod);
app+='\n\n'+mod.trim()+'\n';
for(const t of['DEGREE-SHADOW-CALIBRATOR-V16.9.1F60.94.31.6','degreeShadowCalibratorF6094316','getSamples:()=>lastSamples'])if(!app.includes(t))throw new Error('[F60.94.31.6] bundle invariant missing '+t);
new Function(app);
fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8').replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=1692976');
fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=1692976'))throw new Error('[F60.94.31.6] cache bust failed');
console.log('[AT AI] V16.9.1F60.94.31.6 build complete: chronological shadow calibration for track, maintenance, weather and rail; cache bust 1692976.');