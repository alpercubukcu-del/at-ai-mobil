/* AT AI Mobil — V16.9.1F60.16 Daily Calibration = Career selector path clone
   - Uses the same context/readAll/match/resolve/dialog path as Career Match Selector.
   - Only the final Apply action differs: it starts Daily Calibration instead of Career analysis.
   - Tümü / Tam / İkiz / Aile are bulk-selection buttons; rows always remain visible.
*/
(() => {
'use strict';
if (window.__AT_DAILY_CALIBRATION_CAREER_CLONE_V616__) return;
window.__AT_DAILY_CALIBRATION_CAREER_CLONE_V616__ = true;
const VERSION='DAILY-CALIBRATION-CAREER-CLONE-V16.9.1F60.16',DB='at_ai_tjk_annual_archive_v13',STORE='races',PANEL='dailyCalibrationCareerPanelF616',DIALOG='dailyCalibrationCareerDialogF616',PAGE=100;
let dbp=null,all=[],items=[],draft=new Set(),ctx=null,shown=PAGE,busy=false;
const $=id=>document.getElementById(id),clean=v=>String(v??'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim(),upper=v=>clean(v).toLocaleUpperCase('tr-TR').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/İ/g,'I'),key=v=>upper(v).replace(/[^A-Z0-9]+/g,''),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=n=>Number(n||0).toLocaleString('tr-TR');
function st(){try{if(typeof state==='object'&&state)return state}catch{}return window.state||{}}
function active(){const d=$('analysisDialog');return !!(d&&d.dataset.dailyCalibrationF6018==='1')}
function sameClass(a,b){try{if(typeof window.canonicalClassKeyV125==='function')return window.canonicalClassKeyV125(a)===window.canonicalClassKeyV125(b)}catch{}return key(a)===key(b)}
function sameAge(a,b){return key(a)===key(b)}
function sameTrack(a,b){const A=upper(a),B=upper(b);for(const t of['CIM','KUM','SENTETIK'])if(A.includes(t)&&B.includes(t))return true;return key(a)===key(b)}
function context(){const s=st(),races=Array.isArray(s.races)?s.races:[],sel=clean($('analysisRace')?.value||s.selectedRace),race=races.find(r=>String(r?.no??r?.raceNo)===sel)||(races.length===1?races[0]:null);if(!race)return null;let meta=null;try{if(typeof programRaceMeta==='function')meta=programRaceMeta(race)}catch{}meta=meta||{ok:true,class:race.class||race.yaradi1||'',ageGroup:race.ageGroup||race.yaradi2||'',distance:race.distance||race.mesafe||'',track:race.track||race.pist||''};const cityId=clean(s.city||$('citySelect')?.value),city=clean((Array.isArray(s.cities)?s.cities:[]).find(c=>clean(c?.id)===cityId)?.name)||clean($('citySelect')?.selectedOptions?.[0]?.textContent)||cityId,date=clean(s.date||$('raceDate')?.value),raceNo=Number(race.no||race.raceNo||sel||0);return date&&raceNo?{race,raceNo,city,cityId,date,meta}:null}
function target(c){return`${c.date}|${key(c.city)}|${c.raceNo}`}
function storeKey(c){return`at_ai_daily_calibration_match_selection_v616|${target(c)}`}
function loadSaved(c){try{const a=JSON.parse(localStorage.getItem(storeKey(c))||'[]');return new Set(Array.isArray(a)?a:[])}catch{return new Set()}}
function save(c,s){try{localStorage.setItem(storeKey(c),JSON.stringify([...s]))}catch{}}
function type(c,r){const m=c.meta||{};if(!sameClass(m.class,r.classRaw)||!sameAge(m.ageGroup,r.groupRaw))return'';const city=key(c.city)===key(r.city),dist=Number(m.distance)===Number(r.distance),track=sameTrack(m.track,r.track);if(city&&dist&&track)return'EXACT';if(dist&&track)return'CONDITION_TWIN';if(city)return'RACE_FAMILY';return''}
function score(c,r){try{const m=c.meta||{},C=typeof classSimilarity==='function'?classSimilarity(m.class,r.classRaw):1,A=typeof ageGroupSimilarity==='function'?ageGroupSimilarity(m.ageGroup,r.groupRaw):1,D=typeof distanceSimilarity==='function'?distanceSimilarity(m.distance,r.distance):Math.max(0,1-Math.abs(Number(m.distance)-Number(r.distance))/800),T=typeof trackSimilarity==='function'?trackSimilarity(m.track,r.track):(sameTrack(m.track,r.track)?1:.12),I=typeof citySimilarity==='function'?citySimilarity(c.city,r.city):(key(c.city)===key(r.city)?1:.5);return Math.round(Math.max(0,Math.min(1,C*.30+A*.25+D*.18+T*.17+I*.10))*100)}catch{return 50}}
const label=t=>t==='EXACT'?'Tam':t==='CONDITION_TWIN'?'İkiz':'Aile',rule=t=>t==='EXACT'?'İl + pist + mesafe aynı':t==='CONDITION_TWIN'?'Pist + mesafe aynı · il değişebilir':'İl aynı · pist / mesafe değişebilir',order=t=>t==='EXACT'?0:t==='CONDITION_TWIN'?1:2;
function date(v){const m=clean(v).match(/^(\d{4})-(\d{2})-(\d{2})$/);return m?`${m[3]}.${m[2]}.${m[1]}`:clean(v)}
function openDb(){if(dbp)return dbp;dbp=new Promise(res=>{try{const q=indexedDB.open(DB);q.onsuccess=()=>res(q.result);q.onerror=q.onblocked=()=>{dbp=null;res(null)}}catch{res(null)}});return dbp}
async function readAll(){const db=await openDb();if(!db||!db.objectStoreNames.contains(STORE))return[];return new Promise(res=>{try{const q=db.transaction(STORE,'readonly').objectStore(STORE).getAll();q.onsuccess=()=>res((q.result||[]).map(x=>x?.value).filter(Boolean));q.onerror=()=>res([])}catch{res([])}})}
async function put(r){const db=await openDb();if(!db||!r?.id)return false;return new Promise(res=>{try{const tx=db.transaction(STORE,'readwrite');tx.objectStore(STORE).put({key:r.id,value:r,updatedAt:Date.now()});tx.oncomplete=()=>res(true);tx.onerror=tx.onabort=()=>res(false)}catch{res(false)}})}
async function mapLimit(xs,n,fn){const a=Array.isArray(xs)?xs:[],out=new Array(a.length);let i=0;async function run(){while(true){const k=i++;if(k>=a.length)return;out[k]=await fn(a[k],k)}}await Promise.all(Array.from({length:Math.min(n,a.length||1)},run));return out}
async function raceNo(r){if(Number(r.raceNo)>0)return Number(r.raceNo);try{const q=await fetch(`/api/tjk-race-meta?date=${encodeURIComponent(r.date)}&cityId=${encodeURIComponent(r.cityId||'')}&cityName=${encodeURIComponent(r.city||'')}`,{cache:'no-store'}),d=await q.json(),m=(Array.isArray(d?.races)?d.races:[]).filter(x=>sameClass(x?.class||x?.yaradi1,r.classRaw)&&sameAge(x?.ageGroup||x?.yaradi2,r.groupRaw)&&Number(x?.distance||x?.mesafe||0)===Number(r.distance)&&sameTrack(x?.track||x?.pist,r.track)).map(x=>Number(x?.no||x?.raceNo)).filter(Boolean).sort((a,b)=>a-b);if(q.ok&&m.length){const occ=Math.max(1,Number(r.occurrenceIndex||1));return m[Math.min(occ-1,m.length-1)]}}catch{}const same=all.filter(x=>x?.date===r.date&&key(x?.city)===key(r.city)).sort((a,b)=>Number(a?.page||0)-Number(b?.page||0)||Number(a?.rowIndex||0)-Number(b?.rowIndex||0)),i=same.findIndex(x=>x?.id===r.id);return i>=0?i+1:0}
async function resolve(rows){let done=0;return(await mapLimit(rows,3,async r=>{const n=await raceNo(r);done++;if($('dcmStatusF616'))$('dcmStatusF616').textContent=`Koşu numaraları doğrulanıyor: ${done}/${rows.length}`;if(!n)return null;if(Number(r.raceNo)!==n){r.raceNo=n;r.permanentKey=`${r.date}|${r.cityId||''}|${n}`;r.resolutionMethod=r.resolutionMethod||'DAILY_CALIBRATION_MANUAL_V616';await put(r)}return r})).filter(Boolean)}
function style(){if($('dcmStyleF616'))return;const s=document.createElement('style');s.id='dcmStyleF616';s.textContent=`#${PANEL}{margin:0 0 12px;padding:13px;border:1px solid #376789;border-radius:14px;background:#0d2a40}#${PANEL} h3{margin:0 0 6px;font-size:15px}#${PANEL} p,#${PANEL} small{font-size:11px;line-height:1.45;opacity:.82}#${PANEL} button{width:100%;min-height:46px;font-weight:800}#${DIALOG}{width:min(960px,100vw);height:min(92vh,900px);max-width:none;max-height:none;padding:0;border:1px solid #315d7c;border-radius:16px;background:#071522;color:#eef7ff}#${DIALOG}::backdrop{background:#000b}#${DIALOG} .shell{height:100%;display:flex;flex-direction:column}#${DIALOG} header,#${DIALOG} footer{padding:12px;border-bottom:1px solid #ffffff18}#${DIALOG} footer{border-top:1px solid #ffffff18;border-bottom:0}#${DIALOG} .head{display:flex;justify-content:space-between;gap:8px}#${DIALOG} h2{margin:0;font-size:18px}#${DIALOG} .target,#${DIALOG} .tools{padding:10px 12px;border-bottom:1px solid #ffffff18;font-size:11px;line-height:1.5}#${DIALOG} .tabs{display:grid;grid-template-columns:repeat(4,1fr);gap:6px}#${DIALOG} .actions{display:grid;grid-template-columns:1fr 1fr;gap:6px}#${DIALOG} button{min-height:40px;border-radius:9px;border:1px solid #ffffff24;background:#ffffff0d;color:#eef7ff;font-weight:800}#${DIALOG} button.active,#${DIALOG} .apply{background:#276f9f}#${DIALOG} .list{flex:1;min-height:0;overflow:auto;padding:6px 10px}#${DIALOG} .row{display:grid;grid-template-columns:28px 1fr auto;gap:8px;padding:10px 4px;border-bottom:1px solid #ffffff12}#${DIALOG} .row input{width:20px;height:20px}#${DIALOG} .title{font-size:12px;font-weight:800}#${DIALOG} .sub{font-size:10px;line-height:1.5;opacity:.73}#${DIALOG} .chips{display:flex;gap:4px;flex-wrap:wrap;margin-top:4px}#${DIALOG} .chip,#${DIALOG} .type{padding:3px 6px;border-radius:999px;background:#ffffff12;font-size:9px}#${DIALOG} .type{font-size:10px;font-weight:800;background:#276f9f55}#${DIALOG} .status{font-size:10px;margin-top:7px;opacity:.78}@media(max-width:560px){#${DIALOG}{width:100vw;height:100dvh;border-radius:0;border:0}#${DIALOG} .tabs{grid-template-columns:repeat(3,1fr)}#${DIALOG} .actions{grid-template-columns:1fr 1fr}#${DIALOG} .apply{grid-column:1/-1}}`;document.head.appendChild(s)}
function panel(){const x=document.createElement('section');x.id=PANEL;x.innerHTML=`<h3>🧭 Kalibrasyon Eşleşmeleri · Manuel Seçim</h3><p>Tam / İkiz / Aile eşleşmelerini birlikte gör. <b>İl, pist ve mesafe tek sabit filtre değildir.</b> Bileşik ve Kariyer de seçtiğin bu havuz üzerinden kalibre edilir.</p><button id="dcmOpenF616" type="button">Eşleşmeleri Gör ve Seç</button><small id="dcmMetaF616">Arşiv yalnız Eşleşmeleri Gör ve Seç düğmesine basınca taranır.</small>`;x.querySelector('#dcmOpenF616').onclick=e=>{e.preventDefault();void open()};return x}
function syncLegacyRace(){const main=$('analysisRace'),legacy=$('xcalRace');if(!legacy)return;let wanted=clean(main?.value||st().selectedRace);if(!wanted||wanted==='all')return;if(legacy.value!==wanted&&[...legacy.options].some(o=>o.value===wanted)){legacy.value=wanted;legacy.dispatchEvent(new Event('change',{bubbles:true}))}}
function ensure(){style();if(!active())return false;const page=$('dailyCalibrationCleanPageF614');if(!page)return false;let p=$(PANEL);if(!p)p=panel();const oldFirst=page.querySelector(':scope > .dcp-card:not(#dcpCleanupF615)');if(oldFirst&&oldFirst!==p)oldFirst.style.display='none';if(p.parentNode!==page)page.prepend(p);const c=context(),saved=c?loadSaved(c):new Set(),meta=$('dcmMetaF616');if(meta)meta.textContent=c?(saved.size?`${saved.size} seçim kayıtlı · Arşiv yalnız Eşleşmeleri Gör ve Seç düğmesine basınca taranır.`:'Arşiv yalnız Eşleşmeleri Gör ve Seç düğmesine basınca taranır.'):'Önce tek bir koşu seçin.';const old=$('dailyCalibrationMatchDialogF613');try{if(old?.open)old.close()}catch{}bindOldButton();return true}
function dialog(){let d=$(DIALOG);if(d)return d;d=document.createElement('dialog');d.id=DIALOG;d.innerHTML=`<div class="shell"><header><div class="head"><div><small>AT AI SYSTEM · ${VERSION}</small><h2>Kalibrasyon Eşleşmelerini Seç</h2></div><button id="dcmCloseF616">✕</button></div></header><div id="dcmTargetF616" class="target"></div><div class="tools"><div class="tabs">${[['ALL','Tümü'],['EXACT','Tam'],['CONDITION_TWIN','İkiz'],['RACE_FAMILY','Aile']].map(([v,t])=>`<button data-pick="${v}">${t}</button>`).join('')}</div><div id="dcmSummaryF616" class="sub" style="margin-top:7px"></div></div><div id="dcmListF616" class="list"></div><footer><div class="actions"><button id="dcmClearF616">Seçimi Temizle</button><button id="dcmApplyF616" class="apply">Seçimi Uygula ve Hesapla</button></div><div id="dcmStatusF616" class="status"></div></footer></div>`;document.body.appendChild(d);$('dcmCloseF616').onclick=()=>d.close();d.querySelectorAll('[data-pick]').forEach(b=>b.onclick=()=>{const t=b.dataset.pick;draft=new Set(items.filter(x=>t==='ALL'||x.type===t).map(x=>x.row.id));d.querySelectorAll('[data-pick]').forEach(x=>x.classList.toggle('active',x===b));render();$('dcmStatusF616').textContent=`${b.textContent} seçildi · ${draft.size} yarış işaretlendi.`});$('dcmClearF616').onclick=()=>{draft.clear();d.querySelectorAll('[data-pick]').forEach(x=>x.classList.remove('active'));render();$('dcmStatusF616').textContent='Seçim temizlendi.'};$('dcmApplyF616').onclick=()=>void apply();d.onchange=e=>{const x=e.target?.closest?.('[data-id]');if(!x)return;x.checked?draft.add(x.dataset.id):draft.delete(x.dataset.id);summary()};d.onclick=e=>{if(e.target?.id==='dcmMoreF616'){shown+=PAGE;render()}};return d}
function summary(){const e=$('dcmSummaryF616');if(!e)return;const c={EXACT:0,CONDITION_TWIN:0,RACE_FAMILY:0};items.forEach(x=>c[x.type]++);e.textContent=`${items.length} eşleşme · Tam ${c.EXACT} · İkiz ${c.CONDITION_TWIN} · Aile ${c.RACE_FAMILY} · ${items.filter(x=>draft.has(x.row.id)).length} seçili`}
function render(){summary();const box=$('dcmListF616'),rows=items.slice(0,shown);if(!box)return;box.innerHTML=rows.length?rows.map(x=>{const r=x.row,cd=key(ctx.city)!==key(r.city),dd=Number(ctx.meta.distance)!==Number(r.distance),td=!sameTrack(ctx.meta.track,r.track);return`<label class="row"><input type="checkbox" data-id="${esc(r.id)}" ${draft.has(r.id)?'checked':''}><div><div class="title">${esc(date(r.date))} · ${esc(r.city)} · ${esc(r.classRaw)}</div><div class="sub">${esc(r.groupRaw)}${r.raceName?' · '+esc(r.raceName):''}</div><div class="chips"><span class="chip">İl: ${esc(r.city)}${cd?' ↔':''}</span><span class="chip">Pist: ${esc(r.track)}${td?' ↔':''}</span><span class="chip">Mesafe: ${esc(r.distance)} m${dd?' ↔':''}</span>${r.raceNo?`<span class="chip">${r.raceNo}.K</span>`:''}</div><div class="sub"><b>${esc(rule(x.type))}</b></div></div><div><div class="type">${label(x.type)}</div><div class="sub">uyum %${x.score}</div></div></label>`}).join('')+(items.length>shown?`<button id="dcmMoreF616" style="width:100%">Daha Fazla Göster · ${fmt(items.length-shown)} kaldı</button>`:''):'<div class="sub" style="padding:20px">Yüklü yıllarda Tam / İkiz / Aile eşleşmesi yok.</div>'}
async function open(){if(busy)return;ctx=context();const d=dialog();if(!d.open){try{d.showModal()}catch{d.setAttribute('open','')}}if(!ctx){$('dcmStatusF616').textContent='Önce tek bir koşu seçin.';return}busy=true;$('dcmStatusF616').textContent='Yüklü Yıllık Arşiv taranıyor…';try{all=await readAll();items=all.filter(r=>r?.id&&r?.date&&r.date<ctx.date).map(row=>({row,type:type(ctx,row),score:score(ctx,row)})).filter(x=>x.type).sort((a,b)=>order(a.type)-order(b.type)||b.score-a.score||String(b.row.date).localeCompare(String(a.row.date)));const allowed=new Set(items.map(x=>x.row.id)),saved=loadSaved(ctx),global=window.ATAnnualArchiveV13?.selectionSet;draft=new Set([...saved,...(global&&typeof global.values==='function'?[...global]:[])].filter(id=>allowed.has(id)));shown=PAGE;d.querySelectorAll('[data-pick]').forEach(x=>x.classList.remove('active'));const ys=[...new Set(all.map(r=>Number(r?.year||String(r?.date||'').slice(0,4))).filter(Boolean))].sort((a,b)=>a-b);$('dcmTargetF616').innerHTML=`<b>${ctx.raceNo}. Koşu · ${esc(ctx.city)} · ${esc(ctx.date)}</b><br>${esc(ctx.meta.class)} · ${esc(ctx.meta.ageGroup)} · ${esc(ctx.meta.distance)} m ${esc(ctx.meta.track)}<br>Yüklü arşiv: ${ys.length?ys[0]+'–'+ys.at(-1):'yıl yok'} · ${fmt(all.length)} yarış.`;render();$('dcmStatusF616').textContent=items.length?'Üstten Tümü / Tam / İkiz / Aile seçebilir veya yarışları tek tek işaretleyebilirsin.':'Yüklü yıllarda Tam / İkiz / Aile eşleşmesi yok.'}catch(e){$('dcmStatusF616').textContent=e?.message||'Arşiv okunamadı.'}finally{busy=false}}
async function apply(){if(busy||!ctx)return;const pick=items.filter(x=>draft.has(x.row.id));if(!pick.length){$('dcmStatusF616').textContent='En az bir eşleşme seçin.';return}busy=true;$('dcmApplyF616').disabled=true;try{const rows=await resolve(pick.map(x=>x.row));if(!rows.length)throw new Error('Seçilen yarışların koşu numarası doğrulanamadı.');const ids=new Set(rows.map(r=>r.id)),s=window.ATAnnualArchiveV13?.selectionSet||window.__AT_AA_SELECTED_IDS_V134__;if(!s?.clear||!s?.add)throw new Error('Yıllık Arşiv seçim kümesi bulunamadı.');s.clear();ids.forEach(id=>s.add(id));save(ctx,ids);syncLegacyRace();try{window.dispatchEvent(new CustomEvent('at-ai:annual-archive-selection',{detail:{selected:ids.size,targetRaceNo:ctx.raceNo,source:'daily-calibration-career-clone-v616'}}))}catch{}$('dcmStatusF616').textContent=`${ids.size} manuel referans uygulandı; Günlük Kalibrasyon başlatılıyor…`;setTimeout(()=>{try{$(DIALOG)?.close()}catch{}syncLegacyRace();const run=$('xcalRunSelected');if(run)run.click();else $('xcalRace')?.dispatchEvent(new Event('change',{bubbles:true}))},80)}catch(e){$('dcmStatusF616').textContent=e?.message||'Seçim uygulanamadı.'}finally{busy=false;$('dcmApplyF616').disabled=false}}
function bindOldButton(){const b=$('dcpOpenF614');if(b&&!b.dataset.f616){b.dataset.f616='1';b.onclick=e=>{e.preventDefault();void open()}}}
function wake(){if(active())ensure()}
document.addEventListener('click',e=>{if(e.target?.closest?.('[data-view="scenario"]'))setTimeout(wake,40)},true);document.addEventListener('change',e=>{if(e.target?.matches?.('#analysisRace')&&active())setTimeout(wake,0)},true);try{const o=new MutationObserver(wake),d=$('analysisDialog');if(d)o.observe(d,{attributes:true,attributeFilter:['open','data-view','data-daily-calibration-f6018']})}catch{}window.addEventListener('pageshow',()=>setTimeout(wake,40),{passive:true});setInterval(()=>{if(active())bindOldButton()},1000);setTimeout(wake,120);
window.ATDailyCalibrationCareerCloneV616={version:VERSION,open,ensure};
console.info('[AT AI]',VERSION,'active — Daily Calibration now uses the Career Match Selector code path; only Apply target differs.');
})();
