const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f733.cjs');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
if(!fs.existsSync(BASE))throw new Error('[F60.94.31.14] missing '+path.basename(BASE));
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
let app=fs.readFileSync(APP,'utf8');

function replaceBlock(startToken,endToken,replacement,label){
  const start=app.indexOf(startToken),end=app.indexOf(endToken,start);
  if(start<0||end<0||end<=start)throw new Error('[F60.94.31.14] '+label+' target missing');
  app=app.slice(0,start)+replacement+app.slice(end);
}

const ensure=`async function ensureAnalysis(no){let c=currentResult(),date=dateNow(),targetNo=Number(no),pr=programRaceFor(targetNo);const targetRace=()=>((currentResult()?.races||[]).find(r=>Number(r?.no)===targetNo)||null),sameHorse=(x,h)=>Number(x?.no)===Number(h?.no)||fold(x?.name)===fold(h?.name),hasTarget=()=>!!targetRace(),targetComplete=()=>{const rr=targetRace(),expected=pr?.horses||[],got=rr?.horses||[];if(!rr)return false;if(!expected.length)return true;return got.length>=expected.length&&expected.every(h=>got.some(x=>sameHorse(x,h)))},degreeIncomplete=()=>{const rr=targetRace(),expected=pr?.horses||[];if(!rr||!expected.length)return false;return expected.some(h=>{const x=(rr.horses||[]).find(y=>sameHorse(y,h)),dm=x?.degreeModel;return !x||!Number.isFinite(Number(dm?.predictedSec))||!!dm?.fallback||clean(dm?.method)==='YARIS_MEDYAN_FALLBACK'})};if(c?.races?.length&&clean(c?.date||date)===date&&hasTarget()&&targetComplete()){try{if((!c.degreeSpeed||degreeIncomplete())&&window.ATDegreeSpeedF6090?.enrichCurrent)await window.ATDegreeSpeedF6090.enrichCurrent()}catch{}try{window.ATDegreePredictionAuditF609430?.ensureEveryHorse?.(currentResult()||c)}catch{}return currentResult()||c}if(typeof gRunCurrentV1657!=='function')throw new Error('Güncel Analiz motoru hazır değil.');const sel=$('analysisRace');if(!sel)throw new Error('Güncel Analiz yarış seçici hazır değil.');const target=String(targetNo);if(![...(sel.options||[])].some(o=>String(o.value)===target)){if(!pr)throw new Error(targetNo+'. Koşu programda bulunamadı.');const opt=document.createElement('option');opt.value=target;opt.textContent=target+'. Koşu';sel.appendChild(opt)}sel.value=target;if(sel.value!==target)throw new Error(targetNo+'. Koşu Güncel Analiz için seçilemedi.');setPanelStatus(targetNo+'. Koşu · Güncel Analiz tam at listesiyle hazırlanıyor…');await gRunCurrentV1657();c=currentResult();if(!c?.races?.length)throw new Error('Güncel Analiz sonucu oluşmadı.');if(!hasTarget())throw new Error(targetNo+'. Koşu Güncel Analiz tarafından üretilemedi.');if(!targetComplete())throw new Error(targetNo+'. Koşu Güncel Analiz tüm program atlarını üretmedi.');try{if(window.ATDegreeSpeedF6090?.enrichCurrent)await window.ATDegreeSpeedF6090.enrichCurrent()}catch{}try{window.ATDegreePredictionAuditF609430?.ensureEveryHorse?.(c)}catch{}return currentResult()||c}\n`;
replaceBlock('async function ensureAnalysis(no){','async function compute(no){',ensure,'ensureAnalysis');

const degree=`function degreeScores(rows){const isFallback=r=>!!r?.analysis?.degreeModel?.fallback||clean(r?.analysis?.degreeModel?.method)==='YARIS_MEDYAN_FALLBACK',trusted=rows.filter(r=>Number.isFinite(Number(r?.analysis?.degreeModel?.predictedSec))&&!isFallback(r)),map=normalizeLower(trusted,r=>Number(r.analysis.degreeModel.predictedSec)),ranked=[...trusted].sort((a,b)=>Number(a.analysis.degreeModel.predictedSec)-Number(b.analysis.degreeModel.predictedSec)||Number(a.no)-Number(b.no)),rankMap=new Map(ranked.map((r,i)=>[r,i+1]));for(const r of rows){const dm=r?.analysis?.degreeModel||null,pred=Number(dm?.predictedSec),fallback=isFallback(r);r.predictedSec=Number.isFinite(pred)?pred:null;r.degreeFallback=fallback;r.degreeMethod=clean(dm?.method||'');r.degreeRank=!fallback&&rankMap.has(r)?rankMap.get(r):null;r.D=!fallback&&map.has(r)?map.get(r):null;r.degreeReason=!r.analysis?'Güncel Analiz kaydı yok':!Number.isFinite(pred)?'Derece-Hız için yeterli geçmiş örnek yok':fallback?'Yarış medyanı fallback · T puanına katılmadı':(r.degreeMethod||'Derece-Hız tahmini')}}\nfunction fogdTotal(r){const values={F:r.F,O:r.O,G:r.G,D:r.D};let sum=0,coverage=0,count=0;for(const k of Object.keys(WEIGHTS)){const v=values[k];if(Number.isFinite(v)){sum+=v*WEIGHTS[k];coverage+=WEIGHTS[k];count++}}return{score:coverage?Number(sum.toFixed(1)):null,normalized:coverage?Number((sum/coverage).toFixed(1)):null,coverage:Number(coverage.toFixed(2)),count}}\n`;
replaceBlock('function degreeScores(rows){','async function openExisting(name){',degree,'degreeScores');

const audit=`function degreeAudit(r){const pred=Number(r?.predictedSec),actual=Number(r?.actualSec),dRank=Number(r?.degreeRank),finish=Number(r?.actualFinish),trusted=!r?.degreeFallback;return{timeError:trusted&&Number.isFinite(pred)&&pred>0&&Number.isFinite(actual)&&actual>0?Number((actual-pred).toFixed(2)):null,rankError:trusted&&Number.isFinite(dRank)&&dRank>0&&Number.isFinite(finish)&&finish>0?finish-dRank:null}}\n`;
replaceBlock('function degreeAudit(r){','function degreeMismatchText(r){',audit,'degreeAudit');

const compute=`async function compute(no){if(busy)return;busy=true;setButtons(true);try{const date=dateNow(),city=cityNow(),pr=programRaceFor(no);if(!date||!city||!pr)throw new Error('Önce günün programını yükleyin ve bir koşu seçin.');setPanelStatus(no+'. Koşu · Güncel Analiz kontrol ediliyor…');await ensureAnalysis(no);const cr=currentRaceFor(no);if(!cr)throw new Error(no+'. Koşu Güncel Analiz içinde bulunamadı.');const horses=(pr.horses||[]).map(h=>({program:h,analysis:matchAnalysisHorse(cr,h),name:h.name,no:h.no,currentRank:null,F:formScore(h.last6),O:null,G:null,D:null,T:null,context:null}));const order=[...(cr.horses||[])];for(const r of horses){const ix=order.findIndex(x=>Number(x?.no)===Number(r.no)||fold(x?.name)===fold(r.name));r.currentRank=ix>=0?ix+1:null}degreeScores(horses);setPanelStatus(no+'. Koşu · TJK Orijin ve Galop verileri alınıyor 0/'+horses.length+'…');let done=0;const contexts=await mapLimit(horses,3,async r=>{const d=await horseContext(date,r.program);done++;setPanelStatus(no+'. Koşu · TJK Orijin ve Galop verileri '+done+'/'+horses.length+'…');return d});horses.forEach((r,i)=>{if(contexts[i]&&!contexts[i].error){r.context=contexts[i];r.O=Number.isFinite(Number(contexts[i]?.origin?.score))?Number(contexts[i].origin.score):null}});workoutScores(horses);for(const r of horses){const t=fogdTotal(r);r.T=t.score;r.TNormalized=t.normalized;r.componentCoverage=t.coverage;r.componentCount=t.count}horses.sort((a,b)=>(Number(b.T)||-1)-(Number(a.T)||-1)||(Number(b.componentCoverage)||0)-(Number(a.componentCoverage)||0)||Number(a.no)-Number(b.no));horses.forEach((r,i)=>r.totalRank=i+1);const actual=await actualRace(date,city,no);for(const r of horses){const a=matchActual(actual,r.program);r.actualFinish=Number(a?.finish)||null;r.actualDegree=clean(a?.degree)||'';r.actualSec=degreeSec(a?.degree);const dm=degreeAudit(r);r.degreeTimeErrorSec=dm.timeError;r.degreeRankError=dm.rankError;r.signal=signal(r)}const snapshot={key:date+'|'+fold(city)+'|'+Number(no),date,year:Number(date.slice(0,4))||0,city,raceNo:Number(no),weights:WEIGHTS,rows:horses.map(r=>({no:r.no,name:r.name,F:r.F,O:r.O,G:r.G,D:r.D,T:r.T,TNormalized:r.TNormalized,componentCount:r.componentCount,componentCoverage:r.componentCoverage,currentRank:r.currentRank,degreeRank:r.degreeRank,totalRank:r.totalRank,predictedSec:r.predictedSec,degreeFallback:r.degreeFallback,degreeMethod:r.degreeMethod,degreeReason:r.degreeReason,actualFinish:r.actualFinish,actualDegree:r.actualDegree,actualSec:r.actualSec,degreeTimeErrorSec:r.degreeTimeErrorSec,degreeRankError:r.degreeRankError,signal:r.signal,gMeta:r.gMeta,origin:r.context?.origin||null})),generatedAt:new Date().toISOString(),version:VERSION,buildVersion:'F60.94.31.14'};await dbPut(SCORE_DB,'score','races',snapshot);renderResults(pr,horses,actual);setPanelStatus(no+'. Koşu tamamlandı · '+horses.length+' at · T lideri '+(horses[0]?.no||'—')+'-'+(horses[0]?.name||'—')+'.')}catch(e){setPanelStatus('Hata: '+(e?.message||e))}finally{busy=false;setButtons(false)}}\n`;
replaceBlock('async function compute(no){','function renderResults(pr,rows,actual){',compute,'compute');

app=app.replace('d1=rows.find(x=>x.degreeRank===1),t1=','d1=rows.find(x=>x.degreeRank===1&&!x.degreeFallback&&Number.isFinite(x.D)),t1=');
app=app.replace('<th>D</th><th>T</th><th>Güncel</th><th>D Sıra</th>','<th>D</th><th>T</th><th>Veri</th><th>Güncel</th><th>D Sıra</th>');
const oldCells="<td>${Number.isFinite(r.D)?r.D.toFixed(1):'—'}</td><td><b>${Number.isFinite(r.T)?r.T.toFixed(1):'—'}</b></td><td>${r.currentRank||'—'}</td><td>${r.degreeRank||'—'}</td>";
const newCells="<td>${Number.isFinite(r.D)?r.D.toFixed(1):'—'}${r.degreeReason?'<div style=\"font-size:9px;opacity:.62;max-width:150px;margin-top:3px\">'+esc(r.degreeReason)+'</div>':''}</td><td><b>${Number.isFinite(r.T)?r.T.toFixed(1):'—'}</b></td><td><b>${Number(r.componentCount)||0}/4</b><div style=\"font-size:9px;opacity:.62\">%${Math.round((Number(r.componentCoverage)||0)*100)} ağırlık</div></td><td>${r.currentRank||'—'}</td><td>${r.degreeRank||'—'}</td>";
app=app.replace(oldCells,newCells);
app=app.replace('<b>T · Toplam</b>: mevcut bileşenlerin ağırlıklı puanı. Eksik veri sıfır sayılmaz; ağırlık kalan bileşenlere dağıtılır.','<b>T · Toplam</b>: bileşenlerin sabit ağırlıklı puanı. Eksik bileşenin ağırlığı diğerlerine dağıtılmaz; 4/4 tam veri %100 kapsamdır. 3/4 satırda eksik bileşenin ağırlığı T puanına eklenmez. Yarış medyanı D fallback yalnız bilgi amaçlıdır.');

for(const token of['Güncel Analiz tam at listesiyle hazırlanıyor','function fogdTotal(r)','degreeFallback','Yarış medyanı fallback · T puanına katılmadı','buildVersion:\'F60.94.31.14\'','ARCHIVE-EXACT-DATE-V16.9.1F60.94.31.12','FOGD-HISTORY-CALIBRATION-V16.9.1F60.94.31.11'])if(!app.includes(token))throw new Error('[F60.94.31.14] bundle invariant missing '+token);
new Function(app);
fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8').replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=1692984');
fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=1692984'))throw new Error('[F60.94.31.14] cache bust failed');
console.log('[AT AI] V16.9.1F60.94.31.14 build complete: stale/partial Current Analysis repaired; D fallback separated; T uses fixed-weight data coverage.');
