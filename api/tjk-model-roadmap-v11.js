const VERSION = 'TJK-MODEL-ROADMAP-V11.15-COMPACT-MOBILE';
const INTERNAL_RETRIES = 2;
const HISTORY_CONCURRENCY = 8;
const CAREER_CONCURRENCY = 8;
const MODEL_TYPES = ['EXACT', 'CONDITION_TWIN', 'RACE_FAMILY'];

function clean(v=''){return String(v??'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim()}
function upper(v=''){return clean(v).toLocaleUpperCase('tr-TR').normalize('NFKD').replace(/[\u0300-\u036f]/g,'')}
function fullClassKey(v=''){return upper(v).replace(/Y\s*-\s*(\d+)/g,'Y$1').replace(/\s*\/\s*/g,'/').replace(/\s+/g,' ').trim()}
function getBaseUrl(req){const host=clean(req.headers?.['x-forwarded-host'])||clean(req.headers?.host)||'at-ai-mobil.vercel.app',protocol=clean(req.headers?.['x-forwarded-proto'])||(host.includes('localhost')?'http':'https');return`${protocol}://${host}`}
function normalizeType(v=''){const t=upper(v);if(t==='SAME_RACE_FAMILY')return'RACE_FAMILY';return MODEL_TYPES.includes(t)?t:''}
function typeLabel(type){if(type==='EXACT')return'TAM TARİHSEL EŞLEŞME';if(type==='CONDITION_TWIN')return'KOŞUL İKİZİ';return'AYNI YARIŞ AİLESİ'}
function normalizeCity(v=''){return upper(v).replace(/[^A-Z0-9]/g,'')}
function normalizeTrack(v=''){const t=upper(v);if(t.includes('SENTETIK'))return'SENTETIK';if(t.includes('CIM'))return'CIM';if(t.includes('KUM'))return'KUM';return t}
function normalizeDistance(v){if(typeof v==='number'&&Number.isFinite(v))return Math.round(v);const m=clean(v).match(/\d{3,4}/);return m?Number.parseInt(m[0],10):0}
function parseAge(v=''){const t=upper(v).replace(/\s+/g,' ');let breed='';if(t.includes('INGILIZ'))breed='I';else if(t.includes('ARAP'))breed='A';let m=t.match(/(\d+)\s*VE\s*YUKARI/);if(m)return{breed,min:Number(m[1]),max:99};m=t.match(/(\d+)\s*YASLI/);if(m)return{breed,min:Number(m[1]),max:Number(m[1])};m=t.replace(/\s+/g,'').match(/^(\d+)(\+)?([IA])$/);if(m)return{breed:m[3],min:Number(m[1]),max:m[2]?99:Number(m[1])};return{breed,min:null,max:null}}
function ageKey(v=''){const a=parseAge(v);if(a.breed&&a.min!==null&&a.max!==null)return`${a.breed}:${a.min}:${a.max}`;return upper(v).replace(/\s+/g,'')}
function candidateKey(x={}){return[clean(x.date),normalizeCity(x.city),Number(x.raceNo||0)].join('|')}
function candidateModelKey(x={}){return`${candidateKey(x)}|${normalizeType(x.referenceType)}`}

async function fetchJson(url,timeoutMs=40000,attempts=INTERNAL_RETRIES){let last=null;for(let attempt=1;attempt<=attempts;attempt++){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetch(url,{headers:{Accept:'application/json, text/plain, */*','Cache-Control':'no-cache'},signal:c.signal});const text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch{throw new Error(`JSON olmayan cevap (${r.status}): ${text.slice(0,160)}`)}if(!r.ok)throw new Error(data?.error||`HTTP ${r.status}`);return data}catch(e){last=e?.name==='AbortError'?new Error('İstek zaman aşımına uğradı.'):e;if(attempt<attempts)await new Promise(r=>setTimeout(r,180*attempt))}finally{clearTimeout(t)}}throw last||new Error('İstek başarısız.')}
async function mapLimit(items,limit,worker){const list=Array.isArray(items)?items:[],out=new Array(list.length);let cursor=0;async function run(){while(true){const i=cursor++;if(i>=list.length)return;out[i]=await worker(list[i],i)}}await Promise.all(Array.from({length:Math.min(Math.max(1,limit),list.length||1)},()=>run()));return out}

function normalizeHorse(raw={}){const finish=Number(raw.finish??raw.rank??raw.sira??0)||null;return{finish,horseId:clean(raw.horseId??raw.atId??raw.id??''),horseName:clean(raw.horseName??raw.atAdi??raw.name??''),programNo:Number(raw.programNo??raw.number??raw.no??0)||null,margin:clean(raw.margin??raw.fark??raw.distanceBehind??raw.behind??'')||null}}
function chronological(rows=[]){return[...rows].sort((a,b)=>String(a?.isoDate||a?.date||'').localeCompare(String(b?.isoDate||b?.date||'')))}
function numberOrNull(v){const n=Number(v);return Number.isFinite(n)?n:null}
function compactPathRow(row={}){const finish=Number(row?.finish??row?.rank??row?.sira??0)||null,distance=normalizeDistance(row?.distance??row?.mesafe??row?.msf),track=clean(row?.track??row?.pist??''),className=clean(row?.class??row?.raceClass??row?.classRaw??''),raceClass=clean(row?.raceClass??row?.class??row?.classRaw??''),hp=numberOrNull(row?.hp??row?.handicap),weight=numberOrNull(row?.weight??row?.kilo),odds=numberOrNull(row?.odds??row?.ganyan);return{isoDate:clean(row?.isoDate||row?.date||''),date:clean(row?.date||row?.isoDate||''),city:clean(row?.city||''),distance,mesafe:distance,msf:distance,track,pist:track,finish,rank:finish,sira:finish,class:className,raceClass,ageGroup:clean(row?.ageGroup??row?.group??''),hp,weight,degree:clean(row?.degree||row?.derece||''),odds,raceNoName:clean(row?.raceNoName||row?.raceName||'')}}
function compactPath(rows=[]){return chronological(Array.isArray(rows)?rows:[]).map(compactPathRow)}
function compactAudit(career={}){const audit=career?.audit||{},careerTotal=numberOrNull(audit.careerTotal??audit.expectedFirst??career.careerTotal),collectedTotal=numberOrNull(audit.collectedTotal??career.collectedTotal),coverageStatus=clean(audit.coverageStatus||''),warning=clean(audit.warning||'');return{careerTotal,collectedTotal,coverageStatus:coverageStatus||null,warning:warning||null}}
function careerEnvelope(career={}){const rawFull=Array.isArray(career.history)?career.history:[],full=compactPath(rawFull),wins=compactPath(Array.isArray(career.wins)?career.wins:rawFull.filter(r=>Number(r?.finish??r?.rank??r?.sira)===1)),top5=compactPath(Array.isArray(career.top5)?career.top5:rawFull.filter(r=>{const f=Number(r?.finish??r?.rank??r?.sira);return f>=1&&f<=5})),prep=compactPath(Array.isArray(career.preparationPath)?career.preparationPath:top5);return{ok:Boolean(career.ok),error:career?.ok?null:(career?.error||'Kariyer alınamadı.'),cutoffExclusive:career.before||null,fullPathBefore:full,fullPathBeforeCount:full.length,winsBefore:wins,winsBeforeCount:wins.length,top5Before:top5,top5BeforeCount:top5.length,preparationPathBefore:prep,preparationPathBeforeCount:prep.length,analysisMode:career.analysisMode||(wins.length?'WIN_PATH':full.length?'PREPARATION_PATH':'DEBUT'),audit:compactAudit(career),version:career.version||null,compact:true,compactVersion:'COMPACT_MODEL_PAYLOAD_V1691F38',pathRule:'Tarihsel atın referans yarıştan önceki tüm yarışları tek kompakt yolda taşınır; istemci gerekli türev yolları buradan üretir.'}}

function normalizeCandidate(raw={},year=null){const type=normalizeType(raw.referenceType);return{date:clean(raw.date||raw.isoDate||''),city:clean(raw.city||''),raceNo:Number(raw.raceNo||0),sourceYear:Number(raw.sourceYear||year||String(raw.date||'').slice(0,4))||null,anchorDate:clean(raw.anchorDate||''),calendarDayDifference:Number(raw.calendarDayDifference??999),referenceType:type,referenceLabel:raw.referenceLabel||typeLabel(type),transferabilityScore:Number(raw.transferabilityScore??raw.raceConditionSimilarity??100),transferabilityTier:raw.transferabilityTier||null,transferabilityColor:raw.transferabilityColor||null,explanation:raw.explanation||'',distance:normalizeDistance(raw.distance),track:clean(raw.track||''),class:clean(raw.authoritativeClass||raw.class||''),authoritativeClass:clean(raw.authoritativeClass||raw.class||''),ageGroup:clean(raw.ageGroup||''),fullClassVerified:Boolean(raw.fullClassVerified),_history:raw._history||null}}

function verifySnapshot(target,candidate,history){const authoritativeClass=clean(history?.class||candidate.authoritativeClass||''),authoritativeAge=clean(history?.ageGroup||candidate.ageGroup||''),authoritativeDistance=normalizeDistance(history?.distance||candidate.distance),authoritativeTrack=clean(history?.track||candidate.track||''),fullClassMatch=Boolean(authoritativeClass)&&fullClassKey(target.class)===fullClassKey(authoritativeClass),ageMatch=ageKey(target.ageGroup)===ageKey(authoritativeAge),cityMatch=normalizeCity(target.city)===normalizeCity(candidate.city),distanceMatch=Number(target.distance)===Number(authoritativeDistance),trackMatch=normalizeTrack(target.track)===normalizeTrack(authoritativeTrack),type=normalizeType(candidate.referenceType),structuralMatch=type==='EXACT'?cityMatch&&distanceMatch&&trackMatch:type==='RACE_FAMILY'?cityMatch:type==='CONDITION_TWIN'?distanceMatch&&trackMatch:false;return{ok:fullClassMatch&&ageMatch&&structuralMatch,fullClassMatch,ageMatch,cityMatch,distanceMatch,trackMatch,authoritativeClass,authoritativeAge,authoritativeDistance,authoritativeTrack,targetFullClassKey:fullClassKey(target.class),historyFullClassKey:fullClassKey(authoritativeClass)}}

async function fetchHistoricalSnapshot(baseUrl,candidate,cache){const key=candidateKey(candidate);if(cache.has(key))return cache.get(key);const p=(async()=>{const url=new URL('/api/tjk-history',baseUrl);url.searchParams.set('date',candidate.date);url.searchParams.set('city',candidate.city);url.searchParams.set('raceNo',String(candidate.raceNo));return fetchJson(url.toString(),30000,INTERNAL_RETRIES)})();cache.set(key,p);try{return await p}catch(e){cache.delete(key);throw e}}

async function prepareCandidate(baseUrl,target,candidate,historyCache,sourceTrusted){try{const history=candidate._history||await fetchHistoricalSnapshot(baseUrl,candidate,historyCache);if(!history?.ok)return null;const verification=verifySnapshot(target,candidate,history);if(!verification.ok)return null;if(!sourceTrusted&&!verification.fullClassMatch)return null;return{...candidate,class:verification.authoritativeClass,authoritativeClass:verification.authoritativeClass,ageGroup:verification.authoritativeAge,distance:verification.authoritativeDistance,track:verification.authoritativeTrack,fullClassVerified:true,verification,_history:history}}catch{return null}}

function collectSourceCandidates(source){const map=new Map();const add=(raw,year)=>{if(raw?.ok===false)return;const c=normalizeCandidate(raw,year);if(!c.referenceType||!c.date||!c.city||!c.raceNo)return;const key=candidateModelKey(c),prev=map.get(key);if(!prev||Number(c.transferabilityScore||0)>Number(prev.transferabilityScore||0))map.set(key,c)};for(const raw of Array.isArray(source?.historicalRaces)?source.historicalRaces:[])add(raw,raw?.sourceYear);for(const yr of Array.isArray(source?.yearResults)?source.yearResults:[]){if(yr?.best)add(yr.best,yr.year);for(const m of Array.isArray(yr?.matches)?yr.matches:[])add(m,yr.year)}return[...map.values()]}

async function buildHistoricalHorse(baseUrl,horse,date,careerCache){const result={...horse,career:{ok:false,fullPathBefore:[],historyBefore:[],winsBefore:[],top5Before:[],preparationPathBefore:[]}};if(!horse.horseId){result.career.error='At ID bulunamadı.';return result}const key=`${horse.horseId}|${date}`;let p=careerCache.get(key);if(!p){p=(async()=>{const url=new URL('/api/tjk-career-v10',baseUrl);url.searchParams.set('horseId',horse.horseId);url.searchParams.set('before',date);return fetchJson(url.toString(),35000,INTERNAL_RETRIES)})();careerCache.set(key,p)}try{result.career=careerEnvelope(await p);return result}catch(e){careerCache.delete(key);result.career.error=e?.message||'Tarihsel at kariyeri alınamadı.';return result}}

async function buildRaces(baseUrl,candidates){const raceStates=[];for(const c of candidates){const history=c._history;const top3=(Array.isArray(history?.top3)?history.top3:[]).map(normalizeHorse).filter(h=>h.finish>=1&&h.finish<=3).sort((a,b)=>a.finish-b.finish).slice(0,3);raceStates.push({candidate:c,history,top3,careers:new Array(top3.length)})}
const jobs=[];raceStates.forEach((r,ri)=>r.top3.forEach((horse,hi)=>jobs.push({ri,hi,horse,date:r.candidate.date})));const careerCache=new Map(),careerResults=await mapLimit(jobs,CAREER_CONCURRENCY,j=>buildHistoricalHorse(baseUrl,j.horse,j.date,careerCache));for(let i=0;i<jobs.length;i++)raceStates[jobs[i].ri].careers[jobs[i].hi]=careerResults[i];careerCache.clear();return raceStates.map(r=>{const c=r.candidate,h=r.history;if(!r.top3.length)return{...c,ok:false,top3:[],error:'Tarihsel yarışın gerçek ilk 3 verisi bulunamadı.'};return{...c,ok:true,classVerification:'TJK_HISTORY_FULL_CLASS',authoritativeClass:c.authoritativeClass||h.class||null,condition:{class:h.class||c.class||'',ageGroup:h.ageGroup||c.ageGroup||'',distance:h.distance||c.distance||'',track:h.track||c.track||'',raw:h.conditionRaw||null},top3:r.careers,top3Count:r.careers.length,fullClassVerified:true,pathRule:'İlk 3 atın referans yarıştan önceki tüm yarış geçmişi dondurularak kullanılır.',_history:undefined}})}

export default async function handler(req,res){const startedAt=Date.now();try{const baseUrl=getBaseUrl(req),sourceUrl=new URL('/api/tjk-adaptive-roadmap-v102',baseUrl);for(const key of['date','city','class','ageGroup','track','distance','minYear']){const value=clean(req.query?.[key]||'');if(value)sourceUrl.searchParams.set(key,value)}const sourceStarted=Date.now(),source=await fetchJson(sourceUrl.toString(),90000,1),sourceDurationMs=Date.now()-sourceStarted;if(!source?.ok)throw new Error(source?.error||'V10.3 tarihsel tarama alınamadı.');const target={date:clean(source?.target?.date||req.query?.date||''),city:clean(source?.target?.city||req.query?.city||''),class:clean(source?.target?.class||req.query?.class||''),ageGroup:clean(source?.target?.ageGroup||req.query?.ageGroup||''),track:clean(source?.target?.track||req.query?.track||''),distance:normalizeDistance(source?.target?.distance||req.query?.distance)},sourceTrusted=Boolean(source?.rules?.fullClassVerificationFromResultPage),rawCandidates=collectSourceCandidates(source),historyCache=new Map(),historyStarted=Date.now(),prepared=(await mapLimit(rawCandidates,HISTORY_CONCURRENCY,c=>prepareCandidate(baseUrl,target,c,historyCache,sourceTrusted))).filter(Boolean),historyDurationMs=Date.now()-historyStarted,uniqueMap=new Map();for(const c of prepared){const key=candidateModelKey(c),prev=uniqueMap.get(key);if(!prev||Number(c.transferabilityScore||0)>Number(prev.transferabilityScore||0))uniqueMap.set(key,c)}const unique=[...uniqueMap.values()],careerStarted=Date.now(),built=await buildRaces(baseUrl,unique),careerDurationMs=Date.now()-careerStarted;historyCache.clear();const models={EXACT:[],CONDITION_TWIN:[],RACE_FAMILY:[]};for(const race of built){if(!race?.ok)continue;const type=normalizeType(race.referenceType);if(type&&models[type])models[type].push(race)}for(const type of MODEL_TYPES)models[type].sort((a,b)=>Number(b.sourceYear||0)-Number(a.sourceYear||0));res.setHeader('Cache-Control','no-store, max-age=0');return res.status(200).json({ok:true,version:VERSION,sourceVersion:source.version||null,target,classIdentity:{strategy:'PROGRAM_TO_TJK_RESULT_FULL_TEXT',fullClassKey:fullClassKey(target.class),noFamilyReduction:true,noDecoratorRemoval:true},rules:{yearWindow:'target month/day ±45 days, every historical year separately',independentModels:true,modelTypes:MODEL_TYPES,candidateRule:'V10.3 tarafından sonuç sayfasında TAM sınıfla doğrulanmış adaylar tekrar seri doğrulanmaz; history snapshot toplu çekilir ve TAM sınıf tekrar güvenlik kontrolünden geçer.',top3CareerFreeze:'historical horse career only before historical race date',pathComparison:'FULL_PRE_RACE_HISTORY',pathNote:'Kazanan/ikinci/üçüncünün yalnız eski galibiyetleri değil, referans yarıştan önceki bütün yarış yolu kullanılır.'},models,counts:Object.fromEntries(MODEL_TYPES.map(type=>[type,models[type].length])),diagnostics:{sourceYears:Array.isArray(source.yearResults)?source.yearResults.length:0,sourceVerifiedRaces:Array.isArray(source.historicalRaces)?source.historicalRaces.length:0,rawCandidateCount:rawCandidates.length,verifiedCandidateCount:unique.length,builtCount:built.filter(x=>x?.ok).length,failedBuildCount:built.filter(x=>!x?.ok).length,historyConcurrency:HISTORY_CONCURRENCY,careerConcurrency:CAREER_CONCURRENCY,sourceDurationMs,historyDurationMs,careerDurationMs,durationMs:Date.now()-startedAt,warning:unique.length?'':'Tam sınıfı doğrulanmış tarihsel aday üretilemedi.'}})}catch(e){return res.status(500).json({ok:false,version:VERSION,error:e?.message||'V11 model yol haritası hazırlanamadı.',durationMs:Date.now()-startedAt})}}
