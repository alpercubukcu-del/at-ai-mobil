const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f726.cjs');
const MOD=path.join(ROOT,'archive-actual-autocal-v1691f727.js');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
for(const f of[BASE,MOD])if(!fs.existsSync(f))throw new Error('[F60.94.31.7] missing '+path.basename(f));
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
let app=fs.readFileSync(APP,'utf8');
let mod=fs.readFileSync(MOD,'utf8');
mod=mod.replace('if(complete.length>0){setStatus', 'if(complete.length>=100){setStatus');
mod=mod.replace('if(local?.race?.rows?.length)return local;', 'if(local?.race&&isFinishedRace(local.race))return local;');
for(const t of['ARCHIVE-ACTUAL-AUTOCAL-V16.9.1F60.94.31.7','CURSOR_QUERY_F60.94.31.7','AUTO_REAL_RESULT_F60.94.31.7','Otomatik kalibrasyon açık','syncYearSafe','complete.length>=100','isFinishedRace(local.race)'])if(!mod.includes(t))throw new Error('[F60.94.31.7] module invariant missing '+t);
for(const bad of['new MutationObserver','setInterval(','document.addEventListener(\'touchend\'','document.addEventListener(\'pointerup\''])if(mod.includes(bad))throw new Error('[F60.94.31.7] forbidden global loop '+bad);
new Function(mod);
const degreeSig='async function resultRace(date,city,no){';
const degreeHook="async function resultRace(date,city,no){if(window.ATArchiveActualAutoCalF6094317?.getRace){try{const auto=await window.ATArchiveActualAutoCalF6094317.getRace(date,city,no);if(auto)return auto}catch(e){console.warn('[AT AI] F60.94.31.7 degree actual fallback:',e)}}";
if((app.split(degreeSig).length-1)!==1)throw new Error('[F60.94.31.7] degree resultRace signature count mismatch');
app=app.replace(degreeSig,degreeHook);
const fogSig='async function actualRace(date,city,no){';
const fogHook="async function actualRace(date,city,no){if(window.ATArchiveActualAutoCalF6094317?.getRace){try{const auto=await window.ATArchiveActualAutoCalF6094317.getRace(date,city,no);if(auto)return auto}catch(e){console.warn('[AT AI] F60.94.31.7 FOGD actual fallback:',e)}}";
if((app.split(fogSig).length-1)!==1)throw new Error('[F60.94.31.7] FOGD actualRace signature count mismatch');
app=app.replace(fogSig,fogHook);
app+='\n\n'+mod.trim()+'\n';
for(const t of['ATArchiveActualAutoCalF6094317.getRace(date,city,no)','ARCHIVE-ACTUAL-AUTOCAL-V16.9.1F60.94.31.7','complete.length>=100','CURSOR_QUERY_F60.94.31.7','isFinishedRace(local.race)'])if(!app.includes(t))throw new Error('[F60.94.31.7] bundle invariant missing '+t);
new Function(app);
fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8').replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=1692977');
fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=1692977'))throw new Error('[F60.94.31.7] cache bust failed');
console.log('[AT AI] V16.9.1F60.94.31.7 build complete: archive PageNumber loop bypassed; completed result days seed index; actual finish times auto-fetch; calibration auto-rebuilds.');