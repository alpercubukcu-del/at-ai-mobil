const fs = require('fs');
const path = require('path');
const { execFileSync } = require('child_process');

const ROOT = __dirname;
const BASE_WRAPPER = path.join(ROOT, 'build-runtime-v148.cjs');
const GENERATED_WRAPPER = path.join(ROOT, '.build-runtime-v149.wrapper.cjs');

function replaceOnce(source, needle, replacement, label) {
  const first = source.indexOf(needle);
  if (first < 0) throw new Error(`[V14.9] ${label}: hedef bulunamadı.`);
  if (source.indexOf(needle, first + needle.length) >= 0) throw new Error(`[V14.9] ${label}: hedef birden fazla bulundu.`);
  return source.slice(0, first) + replacement + source.slice(first + needle.length);
}

const ticketBetStartsRuntimeCode = `const TICKET_BET_STARTS_VERSION='TICKET-BET-STARTS-V11.0-COMPACT';
const buildTicketsBeforeBetStartsV11=buildTicketsV11;
async function refreshBetStartsV11(){if(!state?.date||!state?.city||!getCityName())return{ok:false,error:'Tarih/şehir seçilmedi.'};try{const res=await fetch('/api/tjk-bet-starts-v11?date='+encodeURIComponent(state.date)+'&cityId='+encodeURIComponent(state.city)+'&cityName='+encodeURIComponent(getCityName()),{cache:'no-store'}),data=await res.json();if(!res.ok||!data?.ok)return{ok:false,error:data?.error||('API '+res.status)};const map=new Map((Array.isArray(data.races)?data.races:[]).map(r=>[String(r.raceNo),Array.isArray(r.betStarts)?r.betStarts:[]]));let assigned=0;state.races=(Array.isArray(state.races)?state.races:[]).map(race=>{const starts=map.get(String(race.no))||[];assigned+=starts.length;return{...race,betStarts:starts};});state.analyses.ticketBetStartsV11={version:TICKET_BET_STARTS_VERSION,date:state.date,city:state.city,startCount:assigned,sourceVersion:data.version||null,generatedAt:new Date().toISOString()};save();return{ok:true,startCount:assigned,data};}catch(e){return{ok:false,error:e?.message||'TJK bahis başlangıçları doğrulanamadı.'};}}
buildTicketsV11=async function(){status('TJK bahis başlangıçları doğrulanıyor…');const d=await refreshBetStartsV11();if(!d.ok){status('Bahis başlangıçları doğrulanamadı: '+d.error);return;}if(!d.startCount){status('TJK resmi başlangıç bilgisi bulunamadı; tahmin yapılmayacak.');return;}return buildTicketsBeforeBetStartsV11();};buildTickets=buildTicketsV11;if($('buildAllBtn'))$('buildAllBtn').onclick=buildTicketsV11;if($('ticketFromAnalysis'))$('ticketFromAnalysis').onclick=buildTicketsV11;`;

const loopGuardRuntimeCode = `const AT_AI_LOOP_GUARD_VERSION='AT-AI-LOOP-GUARD-V11.11-COMPACT',AT_AI_LEG_CONCURRENCY_V1111=2,AT_AI_MODEL_TIMEOUT_MS_V1111=55000,AT_AI_CAREER_TIMEOUT_MS_V1111=35000,AT_AI_RACE_TIMEOUT_MS_V1111=70000;
async function atAiFetchJsonV1111(url,timeoutMs,label){const c=new AbortController(),timer=setTimeout(()=>c.abort(),timeoutMs);try{const res=await fetch(url,{cache:'default',headers:{accept:'application/json'},signal:c.signal}),text=await res.text();let data={};try{data=text?JSON.parse(text):{};}catch{throw new Error(label+': JSON olmayan cevap (API '+res.status+').');}if(!res.ok||!data?.ok)throw new Error(data?.error||label+': API '+res.status);return data;}catch(e){if(e?.name==='AbortError')throw new Error(label+': istek zaman aşımına uğradı.');throw e;}finally{clearTimeout(timer);}}
function atAiPromiseTimeoutV1111(promise,timeoutMs,label){let timer;const timeout=new Promise((_,reject)=>{timer=setTimeout(()=>reject(new Error(label+': hesaplama zaman aşımına uğradı.')),timeoutMs);});return Promise.race([promise,timeout]).finally(()=>clearTimeout(timer));}
function emptyCareerV1111(type,error){return{ok:false,errorType:type,error,roadmap:[],wins:[],top5:[],preparationPath:[],history:[],summary:{totalTop5:0,totalWins:0,first:0,second:0,third:0,fourth:0,fifth:0}};}
fetchCareer=async function(horseId,before){if(!horseId)return emptyCareerV1111('INPUT','At ID bulunamadı.');try{const data=await atAiFetchJsonV1111('/api/tjk-career-v10?horseId='+encodeURIComponent(horseId)+'&before='+encodeURIComponent(before),AT_AI_CAREER_TIMEOUT_MS_V1111,'Kariyer');return typeof normalizeCareerResponse==='function'?normalizeCareerResponse(data):data;}catch(e){return emptyCareerV1111('RETRIEVAL_ERROR',e?.message||'Kariyer alınamadı.');}};
fetchModelRoadmapV11=async function(race){const meta=typeof programRaceMeta==='function'?programRaceMeta(race):{ok:true,class:race.class,ageGroup:race.ageGroup,track:race.track,distance:race.distance};if(!meta?.ok)return{ok:false,error:meta?.error||'Koşu şartları eksik.'};const url='/api/tjk-model-roadmap-v11?date='+encodeURIComponent(state.date)+'&city='+encodeURIComponent(getCityName())+'&class='+encodeURIComponent(meta.class||race.class||'')+'&ageGroup='+encodeURIComponent(meta.ageGroup||race.ageGroup||'')+'&track='+encodeURIComponent(meta.track||race.track||'')+'&distance='+encodeURIComponent(meta.distance||race.distance||'')+'&minYear=2000';try{return await atAiFetchJsonV1111(url,AT_AI_MODEL_TIMEOUT_MS_V1111,'Koşu '+race.no+' tarihsel model');}catch(e){return{ok:false,error:e?.message||'V11 model yol haritası alınamadı.'};}};
prepareManualTicketV117=async function(resetSelections=false){const root=ensureManualRootV117();if(!root||!manualTicketV117.betType||manualTicketV117.busy)return;const plan=resolveBetStartV11(manualTicketV117.betType);if(!plan?.ok){$('manualPlanV117').innerHTML='<div class="manual-error-v117">⚠ '+escapeHtml(plan?.error||'Bahis başlangıcı kurulamadı.')+'</div>';return;}const token=++manualTicketV117.prepToken;manualTicketV117.busy=true;manualTicketV117.plan=plan;manualTicketV117.activeLeg=0;$('manualBetTypeLabelV117').textContent=manualTicketV117.betType;$('manualBetStartLabelV117').textContent=plan.startRace+'. koşudan başlar · Ayaklar '+legRangeTextV117(plan);$('manualPlanV117').classList.remove('empty');$('manualPlanV117').innerHTML='<div class="manual-loading-v117">'+escapeHtml(modelV117().label)+' modeli hazırlanıyor…<br><small id="manualLoopProgressV1111">0 / '+plan.legs.length+' koşu</small></div>';let completed=0;status(manualTicketV117.betType+': sistem seçimleri hesaplanıyor… 0/'+plan.legs.length);try{const pairs=await mapLimitV11(plan.legs,AT_AI_LEG_CONCURRENCY_V1111,async race=>{if(token!==manualTicketV117.prepToken)throw new Error('Önceki hesaplama iptal edildi.');const raceData=await atAiPromiseTimeoutV1111(prepareRaceModelsV11(race,msg=>{if(token===manualTicketV117.prepToken)status(msg+' · '+completed+'/'+plan.legs.length);}),AT_AI_RACE_TIMEOUT_MS_V1111,race.no+'. koşu');completed++;if(token===manualTicketV117.prepToken){const p=$('manualLoopProgressV1111');if(p)p.textContent=completed+' / '+plan.legs.length+' koşu';status(manualTicketV117.betType+': '+completed+'/'+plan.legs.length+' koşu tamamlandı.');}if(!raceData?.roadmapOk)throw new Error(race.no+'. koşu tarihsel modeli tamamlanamadı: '+(raceData?.roadmapError||'Bilinmeyen hata'));return[String(race.no),raceData];});if(token!==manualTicketV117.prepToken)return;manualTicketV117.raceDataMap=new Map(pairs);const ticket=buildOneTicketV11(plan,modelV117(),manualTicketV117.raceDataMap,manualBudgetV117(),manualUnitV117(),manualSinglesV117());manualTicketV117.systemTicket=ticket;if(resetSelections||!manualTicketV117.selections.size){manualTicketV117.selections=new Map();for(const race of plan.legs){const leg=(ticket?.legs||[]).find(x=>String(x.raceNo)===String(race.no));manualTicketV117.selections.set(String(race.no),new Set((leg?.selections||[]).map(x=>String(x.no))));}}renderManualPlanV117();status(manualTicketV117.betType+' hazır · '+plan.startRace+'. koşudan başlar.');}catch(e){if(token===manualTicketV117.prepToken){manualTicketV117.prepToken++;$('manualPlanV117').innerHTML='<div class="manual-error-v117">⚠ '+escapeHtml(e?.message||'Sistem seçimleri hazırlanamadı.')+'<br><small>Döngü durduruldu; yeniden deneyebilirsiniz.</small></div>';status('AT AI hesaplaması durduruldu: '+(e?.message||'Bilinmeyen hata'));}}finally{manualTicketV117.busy=false;}};`;

let wrapper = fs.readFileSync(BASE_WRAPPER, 'utf8');
wrapper = wrapper.replace('.build-runtime-v148.generated.cjs', '.build-runtime-v149.generated.cjs');

const injection = `\nconst ticketBetStartsRuntimeCode = ${JSON.stringify(ticketBetStartsRuntimeCode)};\nconst loopGuardRuntimeCode = ${JSON.stringify(loopGuardRuntimeCode)};\n`;
wrapper = replaceOnce(wrapper, "let source = fs.readFileSync(BASE, 'utf8');", injection + "\nlet source = fs.readFileSync(BASE, 'utf8');", 'runtime declaration injection');

const extraTransforms = `\nsource = replaceOnce(source, \"  'ticket-bet-starts-v11.js',\", \"  '__TICKET_BET_STARTS_COMPAT__',\", 'ticket bet-start runtime listesi');\nsource = replaceOnce(source, \"  'at-ai-loop-guard-v1111.js',\", \"  '__LOOP_GUARD_COMPAT__',\", 'loop guard runtime listesi');\nsource = replaceOnce(source, 'const historicalReferenceV125Compat = \\`', 'const ticketBetStartsCompat = '+JSON.stringify(ticketBetStartsRuntimeCode)+';\\nconst loopGuardCompat = '+JSON.stringify(loopGuardRuntimeCode)+';\\n\\nconst historicalReferenceV125Compat = \\`', 'V14.9 compact declarations');\nsource = replaceOnce(source, \"  runtimeSourceSet.add('auto-resume-v128.js');\", \"  runtimeSourceSet.add('auto-resume-v128.js');\\n  runtimeSourceSet.add('ticket-bet-starts-v11.js');\\n  runtimeSourceSet.add('at-ai-loop-guard-v1111.js');\", 'V14.9 deploy source prune');\nsource = replaceOnce(source, \"    if (file === '__HISTORICAL_V125_COMPAT__') {\", \"    if (file === '__TICKET_BET_STARTS_COMPAT__') {\\n      runtimeChunks.push('\\\\n/* ticket bet-start compact */\\\\n' + ticketBetStartsCompat);\\n      continue;\\n    }\\n    if (file === '__LOOP_GUARD_COMPAT__') {\\n      runtimeChunks.push('\\\\n/* loop guard compact */\\\\n' + loopGuardCompat);\\n      continue;\\n    }\\n    if (file === '__HISTORICAL_V125_COMPAT__') {\", 'V14.9 runtime compact handlers');\nsource = source.replaceAll('V14.8','V14.9').replaceAll('V148','V149');\nsource = source.replace(\"console.log('[AT AI] Pruned full runtime modules: adaptive-history-v101.js; historical-reference-fullpath-v125.js; ticket-models-v11-mode-aware.js; class-match-v1112.js; annual-selected-set-hook-v134.js; bet-start-strict-v118.js; auto-resume-v128.js.');\",\"console.log('[AT AI] Pruned full runtime modules: adaptive-history-v101.js; historical-reference-fullpath-v125.js; ticket-models-v11-mode-aware.js; class-match-v1112.js; annual-selected-set-hook-v134.js; bet-start-strict-v118.js; auto-resume-v128.js; ticket-bet-starts-v11.js; at-ai-loop-guard-v1111.js.');\");\nsource = source.replace(\"console.log('[AT AI] Compact guards retained: raw-path ranking; null-score filter; official bet-start only; reconnect auto-resume; annual selectedIds capture.');\",\"console.log('[AT AI] Compact guards retained: raw-path ranking; null-score filter; official bet-start refresh/resolve; loop timeouts/concurrency; reconnect auto-resume; annual selectedIds capture.');\");\n`;
wrapper = replaceOnce(wrapper, '\nnew Function(source);', extraTransforms + '\nnew Function(source);', 'V14.9 transform injection');

new Function(wrapper);
fs.writeFileSync(GENERATED_WRAPPER, wrapper, 'utf8');
try { execFileSync(process.execPath, [GENERATED_WRAPPER], { cwd:ROOT, stdio:'inherit' }); }
finally { fs.rmSync(GENERATED_WRAPPER, { force:true }); }
