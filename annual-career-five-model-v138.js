/* AT AI Mobil — Annual Archive Five-Model Career V13.8 */
(() => {
'use strict';
if (window.__AT_ANNUAL_CAREER_FIVE_MODEL_V138__) return;
window.__AT_ANNUAL_CAREER_FIVE_MODEL_V138__ = true;

const VERSION='TJK-ANNUAL-ARCHIVE-V13.8';
const ARCHIVE_DB='at_ai_tjk_annual_archive_v13';
const ARCHIVE_STORE='races';
const CAREER_DB='at_ai_tjk_annual_career_v138';
const CAREER_STORE='careers';
const STORAGE_KEY='at_ai_mobil_state_v2';
let archiveDbPromise=null,careerDbPromise=null,busy=false;
const clean=v=>String(v??'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim();
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const upper=v=>clean(v).toLocaleUpperCase('tr-TR').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/İ/g,'I');
const keyText=v=>upper(v).replace(/[^A-Z0-9]+/g,'').trim();
function finite(v){if(v===null||v===undefined||v==='')return null;const n=Number(v);return Number.isFinite(n)?n:null}
function readState(){try{if(typeof state==='object'&&state)return state}catch{}try{const x=localStorage.getItem(STORAGE_KEY);return x?JSON.parse(x):null}catch{return null}}
function selectionSet(){const s=window.__AT_AA_SELECTED_IDS_V134__;return s&&typeof s.values==='function'?s:null}
function openArchiveDb(){if(archiveDbPromise)return archiveDbPromise;archiveDbPromise=new Promise(resolve=>{try{const q=indexedDB.open(ARCHIVE_DB,1);q.onsuccess=()=>resolve(q.result);q.onerror=()=>resolve(null)}catch{resolve(null)}});return archiveDbPromise}
function openCareerDb(){if(careerDbPromise)return careerDbPromise;careerDbPromise=new Promise(resolve=>{try{const q=indexedDB.open(CAREER_DB,1);q.onupgradeneeded=()=>{if(!q.result.objectStoreNames.contains(CAREER_STORE))q.result.createObjectStore(CAREER_STORE,{keyPath:'key'})};q.onsuccess=()=>resolve(q.result);q.onerror=()=>resolve(null)}catch{resolve(null)}});return careerDbPromise}
async function dbGet(dbFn,store,key){const db=await dbFn();if(!db)return null;return new Promise(resolve=>{try{const q=db.transaction(store,'readonly').objectStore(store).get(key);q.onsuccess=()=>resolve(q.result?.value??null);q.onerror=()=>resolve(null)}catch{resolve(null)}})}
async function dbPut(dbFn,store,key,value){const db=await dbFn();if(!db)return false;return new Promise(resolve=>{try{const tx=db.transaction(store,'readwrite');tx.objectStore(store).put({key,value,updatedAt:Date.now()});tx.oncomplete=()=>resolve(true);tx.onerror=()=>resolve(false);tx.onabort=()=>resolve(false)}catch{resolve(false)}})}
async function archiveGet(id){return dbGet(openArchiveDb,ARCHIVE_STORE,id)}
async function selectedRows(){const s=selectionSet();if(!s?.size)return[];const ids=[...s],out=[];let cursor=0;async function worker(){while(true){const i=cursor++;if(i>=ids.length)return;const r=await archiveGet(ids[i]);if(r)out.push(r)}}await Promise.all(Array.from({length:Math.min(8,ids.length||1)},worker));return out.sort((a,b)=>a.date.localeCompare(b.date)||Number(a.raceNo||0)-Number(b.raceNo||0))}
function currentContext(){const s=readState(),races=Array.isArray(s?.races)?s.races:[],selected=String(s?.selectedRace??'');const race=races.find(r=>String(r?.no??r?.raceNo)===selected)||(races.length===1?races[0]:null);if(!race)return null;const cityId=clean(s?.city);const city=clean((Array.isArray(s?.cities)?s.cities:[]).find(c=>clean(c?.id)===cityId)?.name)||clean(document.getElementById('citySelect')?.selectedOptions?.[0]?.textContent)||cityId;let meta;try{meta=typeof programRaceMeta==='function'?programRaceMeta(race):null}catch{}meta=meta||{ok:true,class:race.class||race.yaradi1||'',ageGroup:race.ageGroup||race.yaradi2||'',distance:race.distance||race.mesafe||'',track:race.track||race.pist||''};return{state:s,race,raceNo:Number(race.no||race.raceNo||0),city,date:clean(s?.date||document.getElementById('raceDate')?.value),meta,horses:(Array.isArray(race.horses)?race.horses:[]).filter(h=>h?.id)}}
function iso(row={}){const x=clean(row.isoDate||row.date);const m=x.match(/^(\d{1,2})[./](\d{1,2})[./](\d{4})$/);return m?`${m[3]}-${String(m[2]).padStart(2,'0')}-${String(m[1]).padStart(2,'0')}`:x}
function chronological(rows){return(Array.isArray(rows)?[...rows]:[]).filter(Boolean).sort((a,b)=>iso(a).localeCompare(iso(b)))}
function envelope(data,before){const full=chronological(data?.history||data?.fullPathBefore||data?.roadmap||[]);const wins=chronological(data?.wins||full.filter(x=>Number(x?.finish??x?.rank??x?.sira)===1));const top5=chronological(data?.top5||full.filter(x=>{const f=Number(x?.finish??x?.rank??x?.sira);return f>=1&&f<=5}));return{...data,ok:data?.ok!==false,cutoffExclusive:before,analysisMode:full.length?'FULL_PATH':'DEBUT',roadmap:full,fullPathBefore:full,historyBefore:full,comparisonPathBefore:full,roadmapBefore:full,fullPathBeforeCount:full.length,winsBefore:wins,top5Before:top5,preparationPathBefore:top5}}
async function career(horseId,before){const key=`${clean(horseId)}|${clean(before)}`;const cached=await dbGet(openCareerDb,CAREER_STORE,key);if(cached?.ok&&Array.isArray(cached.fullPathBefore))return cached;const r=await fetch(`/api/tjk-career-v10?horseId=${encodeURIComponent(horseId)}&before=${encodeURIComponent(before)}`,{cache:'no-store'});const d=await r.json();if(!r.ok||d?.ok===false)throw new Error(d?.error||`Kariyer API ${r.status}`);const value=envelope(d,before);await dbPut(openCareerDb,CAREER_STORE,key,value);return value}
async function mapLimit(items,limit,worker){const list=Array.isArray(items)?items:[],out=new Array(list.length);let cursor=0;async function run(){while(true){const i=cursor++;if(i>=list.length)return;out[i]=await worker(list[i],i)}}await Promise.all(Array.from({length:Math.min(Math.max(1,limit),list.length||1)},run));return out}
function sameClass(a,b){try{if(typeof window.canonicalClassKeyV125==='function')return window.canonicalClassKeyV125(a)===window.canonicalClassKeyV125(b)}catch{}return keyText(a)===keyText(b)}
function sameAge(a,b){return keyText(a)===keyText(b)}
function sameTrack(a,b){const A=upper(a),B=upper(b);if(A.includes('CIM')&&B.includes('CIM'))return true;if(A.includes('KUM')&&B.includes('KUM'))return true;if(A.includes('SENTETIK')&&B.includes('SENTETIK'))return true;return keyText(a)===keyText(b)}
function conditionScore(ctx,row){try{const m=ctx.meta||{};const c=typeof classSimilarity==='function'?classSimilarity(m.class,row.classRaw):(sameClass(m.class,row.classRaw)?1:.4);const a=typeof ageGroupSimilarity==='function'?ageGroupSimilarity(m.ageGroup,row.groupRaw):(sameAge(m.ageGroup,row.groupRaw)?1:.4);const d=typeof distanceSimilarity==='function'?distanceSimilarity(m.distance,row.distance):Math.max(0,1-Math.abs(Number(m.distance)-Number(row.distance))/800);const t=typeof trackSimilarity==='function'?trackSimilarity(m.track,row.track):(sameTrack(m.track,row.track)?1:.12);const ci=typeof citySimilarity==='function'?citySimilarity(ctx.city,row.city):(keyText(ctx.city)===keyText(row.city)?1:.5);return Math.round(Math.max(0,Math.min(1,c*.30+a*.25+d*.18+t*.17+ci*.10))*100)}catch{return 50}}
function channel(ctx,row){const m=ctx.meta||{};if(sameClass(m.class,row.classRaw)&&sameAge(m.ageGroup,row.groupRaw)&&Number(m.distance)===Number(row.distance)&&sameTrack(m.track,row.track)&&keyText(ctx.city)===keyText(row.city))return'EXACT';if(sameClass(m.class,row.classRaw)&&sameAge(m.ageGroup,row.groupRaw))return'CONDITION_TWIN';return'RACE_FAMILY'}
async function historicalRace(ctx,row){const hres=await fetch(`/api/tjk-history?date=${encodeURIComponent(row.date)}&city=${encodeURIComponent(row.city)}&raceNo=${encodeURIComponent(row.raceNo)}`,{cache:'no-store'});const h=await hres.json();if(!hres.ok||h?.ok===false)throw new Error(h?.error||`Tarihsel sonuç ${hres.status}`);const top3=await mapLimit(h.top3||[],2,async ref=>{if(!ref?.horseId)return{...ref,career:{ok:false,fullPathBefore:[]}};try{return{...ref,career:await career(ref.horseId,row.date)}}catch(e){return{...ref,career:{ok:false,fullPathBefore:[],error:e?.message||String(e)}}}});const score=conditionScore(ctx,row),type=channel(ctx,row);return{...h,ok:true,date:row.date,city:row.city,raceNo:row.raceNo,sourceYear:Number(row.year||row.date.slice(0,4)),referenceType:type,referenceLabel:type==='EXACT'?'TAM':type==='CONDITION_TWIN'?'İKİZ':'AİLE',raceConditionSimilarity:score,transferabilityScore:score,top3,top3Count:top3.length,annualArchiveId:row.id}}
function fallbackScore(currentCareer,races,useCondition){const path=currentCareer?.fullPathBefore||currentCareer?.roadmap||[],byYear=new Map();for(const race of races){let best=null;for(const ref of race.top3||[]){const rp=ref?.career?.fullPathBefore||[];if(!path.length||!rp.length)continue;const raw=typeof orderedPathSimilarity==='function'?orderedPathSimilarity(path,rp):0,pathScore=Math.round(raw*100),condition=useCondition?Number(race.transferabilityScore||100):100,score=Math.round(pathScore*condition/100);const x={year:race.sourceYear,score,pathScore,conditionScore:condition,historicalHorse:ref.horseName,historicalFinish:ref.finish,raceDate:race.date,raceCity:race.city,raceNo:race.raceNo,currentPathCount:path.length,referencePathCount:rp.length};if(!best||x.score>best.score)best=x}if(best){const prev=byYear.get(best.year);if(!prev||best.score>prev.score)byYear.set(best.year,best)}}const rows=[...byYear.values()].sort((a,b)=>b.year-a.year),strongest=[...rows].sort((a,b)=>b.score-a.score)[0];return{score:strongest?.score??null,strongest,rows,mode:path.length?'FULL_PATH':'DEBUT',strongYears:rows.filter(x=>x.score>=85).length,supportYears:rows.filter(x=>x.score>=70).length,latestScore:rows[0]?.score??null,coverageYears:rows.length}}
function scoreRows(currentCareer,races,useCondition){try{if(typeof scoreRowsV11==='function')return scoreRowsV11(currentCareer,races,useCondition)}catch{}return fallbackScore(currentCareer,races,useCondition)}
function composite(ch){try{if(typeof compositeScoreV11==='function')return compositeScoreV11(ch)}catch{}const w={exact:.4,twin:.25,family:.2,career:.15};let s=0,u=0;const p=[];for(const k of Object.keys(w)){const v=finite(ch[k]?.score);if(v===null)continue;s+=v*w[k];u+=w[k];p.push(k)}return{score:u?Math.round(s/u):null,present:p,missing:Object.keys(w).filter(k=>!p.includes(k))}}
function modelScores(careerData,models){const exact=scoreRows(careerData,models.EXACT,true),twin=scoreRows(careerData,models.CONDITION_TWIN,true),family=scoreRows(careerData,models.RACE_FAMILY,true);const all=[...models.EXACT,...models.CONDITION_TWIN,...models.RACE_FAMILY].filter((r,i,a)=>a.findIndex(x=>x.date===r.date&&x.city===r.city&&Number(x.raceNo)===Number(r.raceNo))===i);const cp=scoreRows(careerData,all,false),comp=composite({exact,twin,family,career:cp});return{exact,twin,family,career:cp,composite:comp,analysisMode:careerData?.analysisMode||'FULL_PATH'}}
function scoreVal(x,id){return finite(id==='composite'?x?.scores?.composite?.score:x?.scores?.[id]?.score)}
function ranking(data,id){return data.horses.map(x=>({...x,displayScore:scoreVal(x,id)})).filter(x=>x.displayScore!==null).sort((a,b)=>b.displayScore-a.displayScore||Number(a.horse.no||999)-Number(b.horse.no||999))}
function modelLabel(id){return({composite:'Bileşik',exact:'Tam',twin:'İkiz',family:'Aile',career:'Kariyer'})[id]||id}
function rowSummary(s){return s?`${s.coverageYears||0} yıl · ${s.strongYears||0} güçlü · ${s.supportYears||0} destek`:''}
function pathSequence(c){const p=c?.fullPathBefore||c?.roadmap||[];return p.length?p.map(x=>Number(x?.finish??x?.rank??x?.sira)||'-').join(' → '):'—'}
function pathTable(c){try{if(typeof roadmapTableHtml==='function')return roadmapTableHtml(c?.fullPathBefore||c?.roadmap||[])}catch{}return''}
function renderYearRows(s){const rows=Array.isArray(s?.rows)?s.rows:[];if(!rows.length)return'<div class="career-model-empty-v112">Karşılaştırılabilir seçili referans yok.</div>';return`<div style="overflow-x:auto"><table style="width:100%;border-collapse:collapse;font-size:11px;min-width:720px"><thead><tr><th style="text-align:left;padding:6px">Yıl</th><th style="text-align:left;padding:6px">Referans</th><th style="text-align:center;padding:6px">Yol uzunluğu</th><th style="text-align:center;padding:6px">Yol</th><th style="text-align:center;padding:6px">Koşul</th><th style="text-align:center;padding:6px">Net</th></tr></thead><tbody>${rows.map(r=>`<tr><td style="padding:6px;border-top:1px solid rgba(255,255,255,.08)">${esc(r.year)}</td><td style="padding:6px;border-top:1px solid rgba(255,255,255,.08)">${esc(r.historicalHorse||'-')} · ${esc(r.raceDate||'')} ${esc(r.raceCity||'')} ${esc(r.raceNo||'')}.K</td><td style="padding:6px;text-align:center;border-top:1px solid rgba(255,255,255,.08)">${esc(r.currentPathCount??'-')} ↔ ${esc(r.referencePathCount??'-')}</td><td style="padding:6px;text-align:center;border-top:1px solid rgba(255,255,255,.08)">%${esc(r.pathScore??'-')}</td><td style="padding:6px;text-align:center;border-top:1px solid rgba(255,255,255,.08)">%${esc(r.conditionScore??100)}</td><td style="padding:6px;text-align:center;border-top:1px solid rgba(255,255,255,.08)"><b>%${esc(r.score??'-')}</b></td></tr>`).join('')}</tbody></table></div>`}
function render(data,ctx,rows){const ids=['composite','exact','twin','family','career'],out=document.getElementById('aaAnalysis');if(!out)return;const counts=data.modelCounts||{};let criteria='';try{if(typeof careerCriteriaNoteV112==='function')criteria=careerCriteriaNoteV112()}catch{}out.innerHTML=`<div class="aa-section"><h3>5 Model Kariyer Yol Haritası · Seçilen Yarışlar</h3><div class="aa-note"><b>${esc(ctx.raceNo)}. Koşu · ${esc(ctx.city)} · ${esc(ctx.date)}</b><br>${esc(ctx.meta.class||'')} · ${esc(ctx.meta.ageGroup||'')} · ${esc(ctx.meta.distance||'')} ${esc(ctx.meta.track||'')}<br><b>${rows.length} tarihsel yarış</b> yalnız yıllık arşiv seçiminizden kullanıldı. Hesaplama V12.6 tam-kariyer 5 Model motorudur.</div>${criteria}<div class="career-model-tabs-v112" id="aaV138Tabs">${ids.map((id,i)=>`<button class="career-model-tab-v112 ${i===0?'active':''}" data-v138-tab="${id}">${modelLabel(id)}</button>`).join('')}</div>${ids.map((id,i)=>{const rank=ranking(data,id);return`<div class="career-model-panel-v112 ${i===0?'active':''}" data-v138-panel="${id}"><div class="career-model-panel-head-v112"><b>${modelLabel(id)}</b>${id==='exact'?`<span>${counts.EXACT||0} yarış</span>`:id==='twin'?`<span>${counts.CONDITION_TWIN||0} yarış</span>`:id==='family'?`<span>${counts.RACE_FAMILY||0} yarış</span>`:`<span>${rows.length} seçili yarış</span>`}</div>${rank.length?rank.map((x,n)=>`<details class="career-model-rank-v112" style="display:block"><summary style="display:grid;grid-template-columns:34px 1fr auto;align-items:center;gap:8px;cursor:pointer"><span class="career-model-rank-no-v112">${n+1}</span><div class="career-model-rank-horse-v112"><b>${esc(x.horse.no)}. ${esc(x.horse.name)}</b><small>Tam yol: ${esc(pathSequence(x.career))} · ${esc(rowSummary(id==='composite'?x.scores.career:x.scores[id]))}</small></div><div class="career-model-rank-score-v112"><strong>%${esc(x.displayScore)}</strong></div></summary><div style="padding:8px 4px 12px"><div style="font-size:11px;opacity:.75;margin:4px 0 7px"><b>Bugünkü atın tam kariyer yolu (${x.career?.fullPathBeforeCount||0} yarış):</b> ${esc(pathSequence(x.career))}</div>${id==='composite'?`<div class="aa-note">Tam ${scoreVal(x,'exact')===null?'—':'%'+scoreVal(x,'exact')} · İkiz ${scoreVal(x,'twin')===null?'—':'%'+scoreVal(x,'twin')} · Aile ${scoreVal(x,'family')===null?'—':'%'+scoreVal(x,'family')} · Kariyer ${scoreVal(x,'career')===null?'—':'%'+scoreVal(x,'career')}</div>${renderYearRows(x.scores.career)}`:renderYearRows(x.scores[id])}${pathTable(x.career)}</div></details>`).join(''):'<div class="career-model-empty-v112">Bu model için seçilmiş tarihsel referans yok.</div>'}</div>`}).join('')}</div><div class="aa-section"><h3>Kullanılan tarihsel yarışlar</h3>${rows.map(r=>`<div class="aa-row-title" style="padding:5px 0">☑ ${esc(r.date)} ${esc(r.city)} ${esc(r.raceNo)}.K · ${esc(r.classRaw)} · ${esc(r.distance)} ${esc(r.track)}</div>`).join('')}</div>`;out.querySelectorAll('[data-v138-tab]').forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.v138Tab;out.querySelectorAll('[data-v138-tab]').forEach(x=>x.classList.toggle('active',x===btn));out.querySelectorAll('[data-v138-panel]').forEach(x=>x.classList.toggle('active',x.dataset.v138Panel===id))}))}
async function run(){if(busy)return;busy=true;const out=document.getElementById('aaAnalysis');if(out)out.innerHTML='<div class="aa-note">Seçilen yarışlar 5 Model tam-kariyer motoruna hazırlanıyor…</div>';try{const ctx=currentContext();if(!ctx?.meta?.ok)throw new Error('Bugünkü programdan tek bir koşu seçin.');const rows=(await selectedRows()).filter(r=>r.raceNo&&r.date<ctx.date);if(!rows.length)throw new Error('Koşu No’su kesinleşmiş en az bir geçmiş yarış seçin.');let done=0;const refs=await mapLimit(rows,2,async row=>{const r=await historicalRace(ctx,row);done++;if(out)out.innerHTML=`<div class="aa-note">Seçili tarihsel yarışlar: ${done}/${rows.length} · İlk 3 tam kariyerleri hazırlanıyor…</div>`;return r});const models={EXACT:[],CONDITION_TWIN:[],RACE_FAMILY:[]};for(const r of refs)models[r.referenceType]?.push(r);let hd=0;const items=await mapLimit(ctx.horses,2,async horse=>{try{const c=await career(horse.id,ctx.date),scores=modelScores(c,models);hd++;if(out)out.innerHTML=`<div class="aa-note">Bugünkü atlar: ${hd}/${ctx.horses.length} tam kariyerle karşılaştırıldı…</div>`;return{horse,career:c,scores}}catch(e){return{horse,career:{ok:false,fullPathBefore:[]},scores:{exact:{score:null},twin:{score:null},family:{score:null},career:{score:null},composite:{score:null}},error:e?.message||String(e)}}});render({horses:items,modelCounts:{EXACT:models.EXACT.length,CONDITION_TWIN:models.CONDITION_TWIN.length,RACE_FAMILY:models.RACE_FAMILY.length}},ctx,rows)}catch(e){if(out)out.innerHTML=`<div class="aa-note" style="color:#ffbd82">${esc(e?.message||e)}</div>`}finally{busy=false}}
function updateVersion(){const el=document.querySelector('#tjkAnnualArchiveDialog .aa-eyebrow');if(el)el.textContent=`AT AI SYSTEM · ${VERSION}`}
const observer=new MutationObserver(updateVersion);observer.observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('load',updateVersion);updateVersion();
document.addEventListener('click',event=>{const btn=event.target?.closest?.('#aaRunSelected');if(!btn||event.isTrusted)return;event.preventDefault();event.stopImmediatePropagation();run()},true);
window.ATAnnualCareerFiveModelV138={version:VERSION,run};
console.info('[AT AI]',VERSION,'aktif — yıllık arşiv seçimi + V12.6 5 Model tam kariyer');
})();
