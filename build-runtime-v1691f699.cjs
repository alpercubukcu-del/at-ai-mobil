const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f698.cjs');
const QUICK=path.join(ROOT,'track-maintenance-quick-menu-v1691f699.js');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
if(!fs.existsSync(BASE))throw new Error('[F60.94.7] F60.94.6 base builder missing');
if(!fs.existsSync(QUICK))throw new Error('[F60.94.7] quick maintenance runtime missing');
const quick=fs.readFileSync(QUICK,'utf8');
for(const token of['TRACK-MAINT-QUICK-V16.9.1F60.94.7','tmQuickDialogF60947','window.ATTrackMaintenanceQuickF60947','maintenance opens directly without annual archive'])if(!quick.includes(token))throw new Error('[F60.94.7] quick invariant missing: '+token);
new Function(quick);
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
let app=fs.readFileSync(APP,'utf8');
/* F60.45 still contains a legacy drawer writer. Remove only that function body in the built bundle;
   its annual/daily data preparation engine remains intact. */
const menuStart=app.indexOf('function applyProcessMenu() {');
const menuEnd=menuStart>=0?app.indexOf('\nfunction ensureAll()',menuStart):-1;
if(menuStart<0||menuEnd<0)throw new Error('[F60.94.7] legacy applyProcessMenu block not found');
const legacyBlock=app.slice(menuStart,menuEnd);
for(const token of['1. Günlük Veri Paketi','3. Günlük Koşu Kalibrasyonu','5. Güncel Analiz'])if(!legacyBlock.includes(token))throw new Error('[F60.94.7] legacy menu signature missing: '+token);
app=app.slice(0,menuStart)+`function applyProcessMenu() {\n  /* F60.94.7: data planner no longer owns drawer labels/order. */\n  try{window.ATStableAnalysisMenuF60944?.apply?.()}catch{}\n}\n`+app.slice(menuEnd);
if(app.includes("[annual, '1. Günlük Veri Paketi']"))throw new Error('[F60.94.7] legacy menu writer survived build patch');
app+='\n\n'+quick.trim()+'\n';
for(const token of['MENU-AUTHORITY-GUARD-V16.9.1F60.94.6','TRACK-MAINT-QUICK-V16.9.1F60.94.7','8. Pist / Bakım / Hava Arşivi','DEGREE-SPEED-TOP5-V16.9.1F60.94.5'])if(!app.includes(token))throw new Error('[F60.94.7] bundle verification failed: '+token);
new Function(app);
fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8').replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=1692947');
fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=1692947'))throw new Error('[F60.94.7] cache bust failed');
console.log('[AT AI] V16.9.1F60.94.7 build complete: legacy drawer rewrite disabled; Pist/Bakım/Hava opens in a lightweight dedicated dialog.');
