const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f730.cjs');
const PATCH=path.join(ROOT,'fogd-history-calibration-center-v1691f731.js');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
if(!fs.existsSync(BASE))throw new Error('[F60.94.31.11] missing '+path.basename(BASE));
if(!fs.existsSync(PATCH))throw new Error('[F60.94.31.11] missing '+path.basename(PATCH));
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
let app=fs.readFileSync(APP,'utf8');
const patch=fs.readFileSync(PATCH,'utf8');
if(app.includes('__AT_FOGD_HISTORY_CAL_F60943111__'))throw new Error('[F60.94.31.11] history calibration already present');
app+='\n\n'+patch+'\n';
for(const t of['FOGD-HISTORY-CALIBRATION-V16.9.1F60.94.31.11','Seçili Aralığı Hesapla + Kalibre Et','Yılın Eksiklerini Tamamla','Kaldığı Yerden Devam Et','at_ai_fogd_history_progress_v1'])if(!app.includes(t))throw new Error('[F60.94.31.11] bundle invariant missing '+t);
new Function(app);
fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8').replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=1692981');
fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=1692981'))throw new Error('[F60.94.31.11] cache bust failed');
console.log('[AT AI] V16.9.1F60.94.31.11 build complete: city/year/date-range historical FOGD backfill + persistent resume + 5-day calibration batches.');
