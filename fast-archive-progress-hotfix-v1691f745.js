/* AT AI Mobil - F60.94.31.25 fast archive progress / IndexedDB stall hotfix */
(()=>{
'use strict';
if(window.__AT_FAST_ARCHIVE_PROGRESS_HOTFIX_F60943125__)return;
window.__AT_FAST_ARCHIVE_PROGRESS_HOTFIX_F60943125__=true;
const VERSION='AT_FAST_ARCHIVE_PROGRESS_HOTFIX_F60943125';
// legacy build invariant: İndeks ${done}/${pages} — gerçek tarama artık gün gün yapılır.
const QUERY_API='/api/tjk-race-query-v1',RESULT_API='/api/tjk-day-results-v17';
const DAY_DB='at_ai_tjk_real_day_index_v2',DAY_STORE='days',DAY_META='meta';
const RESULT_DB='at_ai_tjk_annual_results_v1',RESULT_RACES='races',RESULT_DAYS='days',RESULT_META='meta';
const PAGE_SIZE=50,QUERY_WORKERS=6,RESULT_WORKERS=4;
const CITIES=[['ADANA','Adana',1],['IZMIR','İzmir',2],['ISTANBUL','İstanbul',3],['BURSA','Bursa',4],['ANKARA','Ankara',5],['SANLIURFA','Şanlıurfa',6],['ELAZIG','Elazığ',7],['DIYARBAKIR','Diyarbakır',8],['KOCAELI','Kocaeli',9],['ANTALYA','Antalya',10]];
const CM=Object.fromEntries(CITIES.map(x=>[x[0],{key:x[0],name:x[1],id:x[2]}]));
const $=id=>document.getElementById(id);
const clean=v=>String(v??'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim();
const fold=v=>clean(v).toLocaleUpperCase('tr-TR').normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/İ/g,'I').replace(/[^A-Z0-9]+/g,'');
const clone=v=>{try{return typeof structuredClone==='function'?structuredClone(v):JSON.parse(JSON.stringify(v))}catch{return v}};
const currentYear=()=>new Date().getFullYear();
const today=()=>{const d=new Date();return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const dayKey=(d,c)=>`${clean(d)}|${fold(c)}`;
const resultDayKey=(d,c)=>`day|${clean(d)}|${fold(c)}`;
const raceKey=(d,c,n)=>`result|${clean(d)}|${fold(c)}|${Number(n)||0}`;
let running=false,stopRequested=false,dayDbPromise=null,resultDbPromise=null;
function status(text,pct=null){const s=$('rrFastStatusF60943121');if(s)s.textContent=String(text||'');if(pct!==null){const b=$('rrFastBarF60943121');if(b)b.style.width=`${Math.max(0,Math.min(100,Number(pct)||0))}%`}}
function busy(on){running=!!on;for(const id of['rrFastRunF60943121','rrFastCitiesF60943121']){const b=$(id);if(b)b.disabled=!!on}const stop=$('rrFastStopF60943121');if(stop)stop.disabled=!on}
function years(){const a=Number($('rrFastYearFromF60943121')?.value),b=Number($('rrFastYearToF60943121')?.value),m=currentYear();if(!Number.isInteger(a)||!Number.isInteger(b)||a<1800||b<1800||a>m||b>m)return null;return[Math.min(a,b),Math.max(a,b)]}
function selectedKeys(){const api=window.ATRealArchiveFastMultiF60943121;let list=[];try{list=api?.getSelectedCities?.()||[]}catch{}const set=new Set(list.map(fold).filter(x=>CM[x]));return set.size?set:new Set()}
function selectedSummary(set){if(set.size===CITIES.length)return'Tüm iller';return CITIES.filter(x=>set.has(x[0])).map(x=>x[1]).join(', ')}
function timeout(p,ms,label){return Promise.race([Promise.resolve(p),new Promise((_,reject)=>setTimeout(()=>reject(new Error(`${label} ${Math.round(ms/1000)} sn içinde tamamlanmadı`)),ms))])}
function openDb(name,setup){return timeout(new Promise(resolve=>{let req;try{req=indexedDB.open(name,1)}catch{return resolve(null)}let settled=false;const done=v=>{if(settled)return;settled=true;resolve(v)};req.onupgradeneeded=()=>{try{setup(req.result,req.transaction)}catch(e){console.warn('[AT AI F60.94.31.25] db setup',name,e)}};req.onsuccess=()=>done(req.result);req.onerror=()=>done(null);req.onblocked=()=>done(null)}),4500,`${name} açılışı`).catch(()=>null)}
async function dayDb(){if(dayDbPromise)return dayDbPromise;dayDbPromise=openDb(DAY_DB,(db,tx)=>{let s=db.objectStoreNames.contains(DAY_STORE)?tx.objectStore(DAY_STORE):db.createObjectStore(DAY_STORE,{keyPath:'key'});if(!s.indexNames.contains('year'))s.createIndex('year','year',{unique:false});if(!s.indexNames.contains('date'))s.createIndex('date','date',{unique:false});if(!s.indexNames.contains('city'))s.createIndex('city','cityKey',{unique:false});if(!db.objectStoreNames.contains(DAY_META))db.createObjectStore(DAY_META,{keyPath:'key'})});const db=await dayDbPromise;if(!db)dayDbPromise=null;return db}
async function resultDb(){if(resultDbPromise)return resultDbPromise;resultDbPromise=openDb(RESULT_DB,(db,tx)=>{let r=db.objectStoreNames.contains(RESULT_RACES)?tx.objectStore(RESULT_RACES):db.createObjectStore(RESULT_RACES,{keyPath:'key'});if(!r.indexNames.contains('year'))r.createIndex('year','year',{unique:false});if(!r.indexNames.contains('date'))r.createIndex('date','date',{unique:false});let d=db.objectStoreNames.contains(RESULT_DAYS)?tx.objectStore(RESULT_DAYS):db.createObjectStore(RESULT_DAYS,{keyPath:'key'});if(!d.indexNames.contains('year'))d.createIndex('year','year',{unique:false});if(!d.indexNames.contains('date'))d.createIndex('date','date',{unique:false});if(!db.objectStoreNames.contains(RESULT_META))db.createObjectStore(RESULT_META,{keyPath:'key'})});const db=await resultDbPromise;if(!db)resultDbPromise=null;return db}
async function apiJson(url,retries=1){let last;for(let n=0;n<=retries;n++){const c=new AbortController(),timer=setTimeout(()=>c.abort(),35000);try{const r=await fetch(url,{cache:'no-store',headers:{accept:'application/json'},signal:c.signal,__atFastRealArchive:true});let d=null;try{d=await r.json()}catch{}if(!r.ok||d?.ok===false)throw new Error(d?.error||`API ${r.status}`);return d}catch(e){last=e;if(n<retries)await new Promise(r=>setTimeout(r,500*(n+1)))}finally{clearTimeout(timer)}}throw last||new Error('Veri alınamadı')}
async function qpage(start,end,page){const u=new URL(QUERY_API,location.origin);u.searchParams.set('start',start);u.searchParams.set('end',end);u.searchParams.set('page',String(page));return apiJson(u.pathname+u.search)}
async function fetchResult(g){const u=new URL(RESULT_API,location.origin);u.searchParams.set('date',g.date);u.searchParams.set('city',g.city);u.searchParams.set('cityId',String(g.cityId));return apiJson(u.pathname+u.search)}
async function mapLimit(items,limit,fn){let cursor=0;async function worker(){for(;;){if(stopRequested)return;const i=cursor++;if(i>=items.length)return;await fn(items[i],i)}}await Promise.all(Array.from({length:Math.min(limit,Math.max(1,items.length))},worker))}
function ingestRows(map,rows){for(const r of rows||[]){const date=clean(r?.date),city=clean(r?.city),info=CM[fold(city)];if(!date||!info)continue;map.set(dayKey(date,city),{key:dayKey(date,city),date,year:Number(date.slice(0,4)),city,cityKey:info.key,cityId:info.id,source:'TJK_KOSU_SORGULAMA_FAST_F60943125',updatedAt:new Date().toISOString()})}}
async function saveFullIndex(rows,year,total,pages){const db=await dayDb();if(!db){console.warn('[AT AI F60.94.31.25] day index db unavailable');return false}return timeout(new Promise(resolve=>{try{const tx=db.transaction([DAY_STORE,DAY_META],'readwrite'),os=tx.objectStore(DAY_STORE),ms=tx.objectStore(DAY_META);for(const v of rows)os.put(v);ms.put({key:`year:${year}:scan`,year,status:'complete',total,pages,nextPage:0,dayCount:rows.length,source:'FAST_F60943125',version:VERSION,updatedAt:new Date().toISOString()});tx.oncomplete=()=>resolve(true);tx.onerror=tx.onabort=()=>resolve(false)}catch{resolve(false)}}),8000,'Gün indeksi yazımı').catch(()=>false)}
async function scanYear(year,selected){
  const start=`${year}-01-01`,end=year===currentYear()?today():`${year}-12-31`,days=new Map();
  const dates=[];for(let d=new Date(start+'T12:00:00');d<=new Date(end+'T12:00:00');d.setDate(d.getDate()+1))dates.push(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`);
  let done=0,totalRows=0;
  const selectedCount=()=>[...days.values()].filter(x=>selected.has(x.cityKey)).length;
  status(`${year}: Koşu Sorgulama gün gün taranıyor · 0/${dates.length}`,2);
  await mapLimit(dates,QUERY_WORKERS,async date=>{
    const q=await qpage(date,date,0);
    ingestRows(days,q?.rows||[]);
    totalRows+=Number(q?.rows?.length||0);done++;
    status(`${year}: Gün ${done}/${dates.length} · ${selectedCount()} seçili gün/şehir · ${QUERY_WORKERS} paralel`,4+Math.round(done/Math.max(1,dates.length)*26));
  });
  if(stopRequested)throw new Error('Kullanıcı tarafından durduruldu');
  const all=[...days.values()].sort((a,b)=>a.date.localeCompare(b.date)||a.city.localeCompare(b.city,'tr'));
  status(`${year}: Gün/şehir indeksi tamamlandı · ${all.length} toplantı · yerel indekse yazılıyor…`,31);
  const saved=await saveFullIndex(all,year,totalRows,dates.length),picked=all.filter(x=>selected.has(x.cityKey));
  status(`${year}: Gün taraması ${dates.length}/${dates.length} tamam · ${picked.length} seçili gün/şehir${saved?'':' · yerel indeks yazımı atlandı'}`,33);
  return picked;
}
async function completedKeys(db,days){if(!db||!days.length)return new Set();return timeout(new Promise(resolve=>{const out=new Set();try{const tx=db.transaction(RESULT_DAYS,'readonly'),os=tx.objectStore(RESULT_DAYS);let left=days.length;for(const g of days){const key=resultDayKey(g.date,g.city),q=os.get(key);q.onsuccess=()=>{const v=q.result;if(v?.status==='complete'&&Number(v?.raceCount)>0)out.add(key);if(--left===0)resolve(out)};q.onerror=()=>{if(--left===0)resolve(out)}}tx.onabort=tx.onerror=()=>resolve(out)}catch{resolve(out)}}),6000,'Hazır sonuç kontrolü').catch(()=>new Set())}
async function saveResultDay(db,g,data){const races=Array.isArray(data?.races)?data.races:[];if(!races.length)throw new Error('Günlük sonuçta yarış bulunamadı');return timeout(new Promise((resolve,reject)=>{try{const tx=db.transaction([RESULT_RACES,RESULT_DAYS],'readwrite'),rs=tx.objectStore(RESULT_RACES),ds=tx.objectStore(RESULT_DAYS),now=new Date().toISOString();for(const r of races){const no=Number(r?.no||r?.raceNo||0);if(no)rs.put({key:raceKey(g.date,g.city,no),year:g.year,date:g.date,city:g.city,cityId:g.cityId,raceNo:no,source:'KOSU_SORGULAMA_FAST_F60943125',version:VERSION,updatedAt:now,race:clone(r)})}ds.put({key:resultDayKey(g.date,g.city),year:g.year,date:g.date,city:g.city,cityId:g.cityId,raceCount:races.length,expectedRaceCount:races.length,status:'complete',source:'KOSU_SORGULAMA_FAST_F60943125',sourceVersion:data?.version||'',era:data?.era||data?.requestedEra||'',updatedAt:now});tx.oncomplete=()=>resolve(races.length);tx.onerror=tx.onabort=()=>reject(tx.error||new Error('Sonuç arşivi yazılamadı'))}catch(e){reject(e)}}),9000,'Sonuç arşivi yazımı')}
async function markError(db,g,e){try{await timeout(new Promise(resolve=>{const tx=db.transaction(RESULT_DAYS,'readwrite');tx.objectStore(RESULT_DAYS).put({key:resultDayKey(g.date,g.city),year:g.year,date:g.date,city:g.city,cityId:g.cityId,raceCount:0,status:'error',source:'KOSU_SORGULAMA_FAST_F60943125',error:e?.message||String(e),updatedAt:new Date().toISOString()});tx.oncomplete=tx.onerror=tx.onabort=()=>resolve()}),3000,'Hata kaydı')}catch{}}
async function downloadYear(year,days){status(`${year}: Yerel sonuç arşivi açılıyor…`,34);const db=await resultDb();if(!db)throw new Error('Yerel Gerçek Sonuç Arşivi açılamadı. Chrome depolamasını kontrol edin.');status(`${year}: Hazır sonuçlar hızlı kontrol ediliyor…`,35);const ready=await completedKeys(db,days);const todo=days.filter(g=>!ready.has(resultDayKey(g.date,g.city)));let pos=0,done=0,errors=0,races=0;if(!todo.length)return{done:0,skipped:ready.size,errors:0,races:0};async function worker(){for(;;){if(stopRequested)return;const i=pos++;if(i>=todo.length)return;const g=todo[i];status(`${year}: Sonuç ${done+1}/${todo.length} · ${g.date} ${g.city} · ${RESULT_WORKERS} paralel`,36+Math.round(done/Math.max(1,todo.length)*62));try{const data=await fetchResult(g);races+=await saveResultDay(db,g,data)}catch(e){errors++;await markError(db,g,e)}done++;status(`${year}: Sonuç ${done}/${todo.length} · ${errors} hata · ${RESULT_WORKERS} paralel`,36+Math.round(done/Math.max(1,todo.length)*62))}}await Promise.all(Array.from({length:Math.min(RESULT_WORKERS,Math.max(1,todo.length))},worker));if(stopRequested)throw new Error('Kullanıcı tarafından durduruldu');try{await timeout(new Promise(resolve=>{const tx=db.transaction(RESULT_META,'readwrite');tx.objectStore(RESULT_META).put({key:`fast:${year}`,year,status:errors?'partial':'complete',dayCount:days.length,downloadedDayCount:done,skippedDayCount:ready.size,newRaceRows:races,errorCount:errors,source:'KOSU_SORGULAMA_FAST_F60943125',version:VERSION,updatedAt:new Date().toISOString()});tx.oncomplete=tx.onerror=tx.onabort=()=>resolve()}),3000,'Meta yazımı')}catch{}return{done,skipped:ready.size,errors,races}}
async function refresh(){try{await window.ATAnnualResultsArchiveV661?.refresh?.()}catch{}try{await window.ATRealDayHistoryF609430?.renderInventory?.()}catch{}try{await window.ATRealRaceArchiveF6093?.refresh?.()}catch{}try{window.dispatchEvent(new CustomEvent('at-ai:real-race-archive-updated',{detail:{version:VERSION,source:'KOSU_SORGULAMA_FAST_F60943125'}}))}catch{}}
async function run(){if(running)return;const yr=years(),selected=selectedKeys();if(!yr)return status('Geçerli yıl aralığı seçin.',0);if(!selected.size)return status('En az bir il seçin.',0);try{window.ATRealArchiveFastMultiF60943121?.stop?.()}catch{}stopRequested=false;busy(true);try{for(let y=yr[0];y<=yr[1];y++){if(stopRequested)break;status(`${y}: Hızlı yöntem başlıyor · ${selectedSummary(selected)}`,1);const days=await scanYear(y,selected);if(!days.length){status(`${y}: seçilen illerde gerçekleşmiş yarış günü bulunamadı.`,100);continue}const r=await downloadYear(y,days);await refresh();status(`${y} tamamlandı · ${days.length} gün/şehir · ${r.skipped} önceden hazır · ${r.done-r.errors} indirildi · ${r.errors} hata`,100)}}catch(e){status(`Hızlı yöntem durdu: ${e?.message||e}. Alttaki mevcut yöntem kullanılabilir.`,0)}finally{busy(false);await refresh()}}
function install(){const runBtn=$('rrFastRunF60943121'),stopBtn=$('rrFastStopF60943121');if(!runBtn||!stopBtn)return false;if(runBtn.dataset.fastProgressFix==='F60943125')return true;runBtn.dataset.fastProgressFix='F60943125';runBtn.onclick=e=>{e.preventDefault();e.stopPropagation();void run()};stopBtn.onclick=e=>{e.preventDefault();e.stopPropagation();stopRequested=true;try{window.ATRealArchiveFastMultiF60943121?.stop?.()}catch{}status('Durdurma istendi · tamamlanan kayıtlar korunuyor…')};const note=$('rrFastStatusF60943121');if(note&&!running)note.textContent='Hazır · F60.94.31.25. Başlayınca İndeks X/Y ve Sonuç X/Y sayaçları görünecek.';return true}
function boot(){if(install())return;const obs=new MutationObserver(()=>{if(install())obs.disconnect()});obs.observe(document.documentElement,{childList:true,subtree:true});for(const ms of[100,300,700,1500,3000,6000])setTimeout(()=>{if(install())obs.disconnect()},ms);setTimeout(()=>obs.disconnect(),12000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
window.ATFastArchiveProgressHotfixF60943125={version:VERSION,run,stop:()=>{stopRequested=true},install};
console.info('[AT AI]',VERSION,'active - network-first race-query scan, visible index/result counters, IndexedDB hard timeouts.');
})();
