const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f724.cjs');
const MOD=path.join(ROOT,'degree-calibration-audit-v1691f725.js');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
for(const f of[BASE,MOD])if(!fs.existsSync(f))throw new Error('[F60.94.31.5] missing '+path.basename(f));
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
let app=fs.readFileSync(APP,'utf8');
const oldStats=/async function statsYear\(y\)\{const days=await rowsByYear\(DAY_DB,DAY_STORE,y\),races=await rowsByYear\(RESULT_DB,RESULT_RACES,y\),resultDays=await rowsByYear\(RESULT_DB,RESULT_DAYS,y\),complete=resultDays\.filter\(x=>x\.status==='complete'&&Number\(x\.raceCount\)>0\)\.length;return\{year:Number\(y\),dayCount:days\.length,raceCount:races\.length,complete,pending:Math\.max\(0,days\.length-complete\)\}\}/;
if(!oldStats.test(app))throw new Error('[F60.94.31.5] statsYear source not found');
app=app.replace(oldStats,"async function statsYear(y){const days=await rowsByYear(DAY_DB,DAY_STORE,y),races=await rowsByYear(RESULT_DB,RESULT_RACES,y),resultDays=await rowsByYear(RESULT_DB,RESULT_DAYS,y),complete=resultDays.filter(x=>x.status==='complete'&&Number(x.raceCount)>0).length,discoveryCount=days.length,dayCount=Math.max(discoveryCount,complete);return{year:Number(y),dayCount,discoveryCount,raceCount:races.length,complete,pending:Math.max(0,discoveryCount-complete)}}");
let mod=fs.readFileSync(MOD,'utf8');
for(const t of['DEGREE-CALIBRATION-AUDIT-V16.9.1F60.94.31.5','at_ai_degree_calibration_v1','DERECE-HIZ GERÇEK SONUÇ DENETİMİ','refreshArchiveMeta'])if(!mod.includes(t))throw new Error('[F60.94.31.5] module invariant missing '+t);
for(const bad of['new MutationObserver','setInterval(','document.addEventListener(\'touchend\'','document.addEventListener(\'pointerup\''])if(mod.includes(bad))throw new Error('[F60.94.31.5] forbidden global loop '+bad);
new Function(mod);
app+='\n\n'+mod.trim()+'\n';
for(const t of['DEGREE-CALIBRATION-AUDIT-V16.9.1F60.94.31.5','discoveryCount=days.length','at_ai_degree_calibration_v1'])if(!app.includes(t))throw new Error('[F60.94.31.5] bundle invariant missing '+t);
new Function(app);
fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8').replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=1692975');
fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=1692975'))throw new Error('[F60.94.31.5] cache bust failed');
console.log('[AT AI] V16.9.1F60.94.31.5 build complete: authoritative archive day counts + degree actual calibration audit; cache bust 1692975.');