const fs=require('fs');
const path=require('path');
const {execFileSync}=require('child_process');
const ROOT=__dirname;
const BASE=path.join(ROOT,'build-runtime-v1691f10.cjs');
const PATCH=path.join(ROOT,'career-path-match-explain-v1691f11.js');
const APP=path.join(ROOT,'public','at-ai-app-v142.js');
const INDEX=path.join(ROOT,'public','index.html');
for(const f of [BASE,PATCH])if(!fs.existsSync(f))throw new Error(`[V16.9.1F11] Eksik dosya: ${path.basename(f)}`);
execFileSync(process.execPath,[BASE],{cwd:ROOT,stdio:'inherit'});
if(!fs.existsSync(APP)||!fs.existsSync(INDEX))throw new Error('[V16.9.1F11] Önceki build çıktısı bulunamadı.');
let app=fs.readFileSync(APP,'utf8');
app+=`\n${fs.readFileSync(PATCH,'utf8')}\n`;
for(const token of ['CAREER-PATH-MATCH-EXPLAIN-V16.9.1F11','UYUMU OLUŞTURAN KOŞU EŞLEŞMELERİ','COUPON-MANUAL-NO-FIVE-MODEL-V16.9.1F10','ARCHIVE-STATE-REUSE-V16.9.1F9'])if(!app.includes(token))throw new Error(`[V16.9.1F11] Doğrulama başarısız: ${token}`);
new Function(app);fs.writeFileSync(APP,app,'utf8');
let html=fs.readFileSync(INDEX,'utf8');html=html.replace(/\/at-ai-app-v142\.js\?v=\d+/,'/at-ai-app-v142.js?v=169112');fs.writeFileSync(INDEX,html,'utf8');
if(!html.includes('/at-ai-app-v142.js?v=169112'))throw new Error('[V16.9.1F11] cache-bust güncellenemedi.');
console.log('[AT AI] V16.9.1F11 build tamamlandı: Kariyer/Hazırlık puanı değişmeden, tarihsel referansla uyumu oluşturan en güçlü koşu çiftleri kullanıcı isteğiyle lazy gösterilir.');
