# F60.62 — Takvim + Çoklu Filtre + Eksik Koşu Onarım Tasarımı

Bu çalışma F60.61 production tabanından ayrılmış geliştirme dalındadır. Production hesabı, puanlama, Kariyer 4-paralel istek yapısı ve mevcut arşiv kayıtları değiştirilmez.

## 1. Ortak tarih aralığı

Başlangıç/Bitiş yılı açılır kutuları kaldırılır. Yerine iki takvim alanı kullanılır:

- Başlangıç tarihi (`YYYY-MM-DD`)
- Bitiş tarihi (`YYYY-MM-DD`)

Mobilde tarih alanına dokununca telefonun tarih seçicisi açılır. Görünen metin Türkçe `GG/AA/YYYY` biçimindedir.

Kurallar:
- başlangıç > bitiş ise otomatik yer değiştir;
- hedef yarış tarihi üst sınırdır; tarihsel analizde gelecek tarih kullanma;
- boş başlangıç = arşivdeki ilk tarih;
- boş bitiş = hedef yarıştan bir gün önce;
- yıllık indirme motoru içeride yine yıllara bölünebilir fakat kullanıcı tarih aralığı görür.

## 2. Ortak çoklu filtre modeli

Tüm tarihsel eşleştirme ekranlarında aynı filtre şeması kullanılır:

```js
{
  startDate,
  endDate,
  cities: Set(),
  groups: Set(),
  classes: Set(),
  distances: Set(),
  tracks: Set(),
  tokens: Set()
}
```

Semantik:
- aynı filtre içinde OR: İstanbul VEYA Ankara; 1400 VEYA 1600 VEYA 1800; Çim VEYA Sentetik;
- farklı filtreler arasında AND;
- boş küme = Tümü;
- `Bugünkü Koşudan Doldur` hedef koşu değerlerini seçer, kullanıcı ek kutuları işaretleyerek havuzu genişletir;
- örnek hedef 1600 m: 1400 + 1600 + 1800 birlikte seçilebilir.

Mobil arayüz `<select multiple>` kullanmaz. Checkbox/chip seçiciler kullanılır. Özet satırı örneği:

`İl: İstanbul + Ankara (2) · Mesafe: 1400 + 1600 + 1800 (3) · Pist: Çim + Sentetik (2)`

## 3. Model anlamları korunur

Çoklu seçim model tanımlarını değiştirmez:
- TAM: il + sınıf + yaş/grup + mesafe + pist mevcut kesin tanıma göre tam eşleşir.
- İKİZ: mevcut İkiz tanımı korunur.
- AİLE: mevcut Aile tanımı korunur.
- ÖZEL/MANUEL HAVUZ: kullanıcı çoklu filtreyle özellikle dahil ettiği ancak TAM/İKİZ/AİLE sınıfına girmeyen yarışlar.

Örneğin hedef 1600 m iken seçilen 1400 ve 1800 m yarışlar otomatik olarak TAM sayılmaz. Böylece tarihsel TAM kalibrasyonunun istatistik anlamı bozulmaz.

## 4. Uygulanacak ekranlar

1. `7. Yıllık Yarış Arşivi / Tarihsel yarış seçimi`
   - yıl başlangıç/bitiş -> takvim tarih aralığı;
   - Şehir, Grup, Koşu Cinsi, Mesafe, Pist -> çoklu seçim;
   - Ek Şartlar mevcut çoklu mantıkla devam eder.

2. `Yerli Yarış Sonuç Arşivi`
   - yıl aralığı -> takvim tarih aralığı;
   - yalnız seçili tarih aralığındaki eksik/yarım kayıtlar güncellenir;
   - tamam kayıtlar tekrar indirilmez.

3. `3. Kariyer Yol Haritası / Kariyer Eşleşmeleri`
   - tarih + il + mesafe + pist + grup + sınıf çoklu filtreleri;
   - TAM/İKİZ/AİLE sekmeleri korunur;
   - genişletilmiş havuz için ÖZEL/MANUEL sekmesi eklenir;
   - hesaplama puanı değişmez; yalnız seçilen tarihsel referans havuzu değişir.

4. `4. Model Kalibrasyonu`
   - başlangıç/bitiş yılı -> takvim;
   - il/mesafe/pist/grup/sınıf çoklu seçim;
   - gerçek kazanan sıra kalibrasyonu sadece kendi sınıf etiketiyle saklanır;
   - TAM istatistiği yalnız gerçek TAM eşleşmelerden üretilir.

5. Aynı tip tarihsel filtre kullanan diğer ekranlar ortak bileşene geçirilir; yeni ekranların ayrı filtre mantığı yazmasına izin verilmez.

## 5. Eksik Koşu Onarım Merkezi ile entegrasyon

Mevcut indirilen sonuçlar korunur. Yeni katman yalnız eksik program yarışlarını tespit eder.

Her planlanan yarış için:
- `scheduledDate`
- `actualResultDate`
- `repairStatus` = COMPLETE / MISSING / CANDIDATE / MANUAL_MATCH / CANCELLED / LINK_ERROR
- `repairType` = NORMAL / RESCHEDULED / CANCELLED
- `repairSource` = EXISTING / AUTO / MANUAL

Takvim filtresi Onarım Merkezi'nde de kullanılır. Kullanıcı belirli tarih aralığındaki eksikleri inceleyebilir.

Otomatik aday arama sırası: aynı gün -> ±1 gün -> ±3 gün -> ±7 gün. Sınıf, yaş/grup, mesafe, pist ve mümkünse at ortaklığı doğrulanmadan otomatik bağlama yapılmaz. Şüpheli aday kullanıcıya gösterilir.

Manuel onarımda gerçek tarih + şehir veya TJK sonuç bağlantısı girilebilir. Kullanıcı doğru yarışı seçtikten sonra eşleme kalıcı kaydedilir.

## 6. Veri güvenliği

- Mevcut IndexedDB arşivleri silinmez.
- Migrasyon eski kayıtları kopyalamadan/yeniden indirmeden tanır.
- Migrasyon tamamlanmadan eski kayda boş veri yazılmaz.
- Tamamlanmış sonuç yarışı `Eksikleri Güncelle` ile tekrar indirilmez.
- İptal/Yapılmadı olarak işaretlenen yarış sonsuz retry döngüsünden çıkarılır.
- Tarihsel tahminde hedef yarışın kendi sonucu kullanılmaz.

## 7. Test kapısı — production öncesi

Production deploy yapılmadan önce aşağıdakiler doğrulanmalıdır:
- mevcut F60.61 Kariyer hesabı aynı sıralamayı üretir;
- Kariyer Günlük Arşiv ve Güncel Analiz Günlük Arşiv kayıtları bozulmaz;
- 4 paralel Kariyer isteği korunur;
- takvim 01.01.2024–31.12.2025 gibi iki yıllık aralık okuyabilir;
- İstanbul + Ankara çoklu şehir filtresi çalışır;
- 1400 + 1600 + 1800 mesafe havuzu çalışır;
- Çim + Sentetik pist havuzu çalışır;
- TAM etiketi 1400/1800 genişletilmiş yarışları yanlışlıkla TAM yapmaz;
- eski sonuç arşivi migrasyon sonrası aynı kayıt sayısını korur;
- partial/error günler yarış bazında onarım listesine ayrılır.
